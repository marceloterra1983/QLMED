---
phase: 12-desduplica-o-de-c-digo
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/lib/siscomex-client.ts
  - src/app/api/ncm/bulk-sync/route.ts
  - src/app/api/products/sync-anvisa/route.ts
autonomous: false
requirements: [CODEDUP-03]

must_haves:
  truths:
    - "ncm/bulk-sync no longer performs a raw fetch() to SISCOMEX inline in the route handler — it calls a dedicated siscomex-client module, matching the sefaz-client.ts/nsdocs-client.ts pattern"
    - "products/sync-anvisa no longer performs a raw fetch() call inline in the route handler (its previous fetch was a self-referential HTTP loopback call to /api/products, not an external ANVISA call — replaced with a direct in-process function call)"
    - "Both routes return the exact same response shape/status codes as before for equivalent inputs"
  artifacts:
    - path: "src/lib/siscomex-client.ts"
      provides: "fetchSiscomexNomenclature(): Promise<SiscomexItem[]>, SiscomexApiError, SiscomexTimeoutError"
      contains: "export async function fetchSiscomexNomenclature"
    - path: "src/app/api/ncm/bulk-sync/route.ts"
      provides: "Calls fetchSiscomexNomenclature() instead of fetch(SISCOMEX_URL) inline"
      contains: "from '@/lib/siscomex-client'"
    - path: "src/app/api/products/sync-anvisa/route.ts"
      provides: "Calls buildProductsListPayload() directly instead of self-fetching /api/products over HTTP"
      contains: "buildProductsListPayload"
  key_links:
    - from: "src/app/api/ncm/bulk-sync/route.ts"
      to: "src/lib/siscomex-client.ts"
      via: "fetchSiscomexNomenclature()"
      pattern: "fetchSiscomexNomenclature"
    - from: "src/app/api/products/sync-anvisa/route.ts"
      to: "src/lib/product-aggregation.ts"
      via: "import { buildProductsListPayload } from '@/lib/product-aggregation'"
      pattern: "buildProductsListPayload"
---

> **CORREÇÃO PÓS-EXECUÇÃO 12-01 (2026-07-11)**: `buildProductsListPayload`/
> `ProductsListQueryParams` acabaram vivendo em `src/lib/product-aggregation.ts`,
> NÃO em `src/app/api/products/route.ts` — Next.js 15 rejeita exports
> nomeados de arquivos `route.ts` que não sejam handlers HTTP reconhecidos.
> Todas as referências de import abaixo já foram corrigidas para
> `@/lib/product-aggregation`.

<objective>
Close the last 2 direct `fetch()` calls in route handlers (CODEDUP-03 /
ARCH-REMEDIATION-PLAN Fase 5.4): `ncm/bulk-sync/route.ts` calls
`fetch(SISCOMEX_URL)` inline with no dedicated client (unlike SEFAZ/NSDocs,
which each have one), and `products/sync-anvisa/route.ts` calls `fetch()`
inline in a paginating loop against its own `/api/products` endpoint (an
internal HTTP loopback, not an external ANVISA call — see note in Task 2).
Create `siscomex-client.ts` following the `sefaz-client.ts`/`nsdocs-client.ts`
shape, and replace the sync-anvisa self-fetch with a direct in-process call
to the `buildProductsListPayload()` function exported by Plan 12-01.

Purpose: every external HTTP call in the codebase should go through a named,
testable client module — not be scattered inline in route handlers — and
internal loopback HTTP calls between our own routes should become direct
function calls (faster, no cookie-forwarding, no self-inflicted network hop).
Output: `siscomex-client.ts`; both routes refactored with identical
response shapes; a manual smoke-test checkpoint closing out Phase 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Reference pattern: existing dedicated external-integration clients this repo already follows -->
`src/lib/sefaz-client.ts` — `class SefazClient` wraps SOAP/HTTPS with typed request/response interfaces (`SefazDocument`, `DistDFeResponse`), timeout handling, gunzip decoding.
`src/lib/nsdocs-client.ts` — fetch-based client with typed methods, custom error classes (`NsdocsTransientError`, `NsdocsPaginationError`) the caller can catch and branch on.

<!-- Exported by Plan 12-01 — import and call this directly instead of self-fetching -->
```typescript
// src/lib/product-aggregation.ts (after Plan 12-01 — moved here, NOT route.ts, per Next.js route-export constraint)
export interface ProductsListQueryParams {
  page: number; limit: number; search: string; sort: string; order: 'asc' | 'desc';
  useAnvisaLookup: boolean; useIssuedNfeLookup: boolean; onlyMissingAnvisa: boolean;
  exportAll: boolean; dateFrom: string | null; dateTo: string | null;
}
export async function buildProductsListPayload(
  companyId: string,
  params: ProductsListQueryParams,
): Promise<{ products: any[]; summary: any; pagination: any; meta: any }>;
```

<!-- Current inline fetch call to move (ncm/bulk-sync/route.ts, ~line 58) -->
```typescript
const SISCOMEX_URL = 'https://portalunico.siscomex.gov.br/classif/api/publico/nomenclatura/download/json';
const res = await fetch(SISCOMEX_URL, { signal: controller.signal, headers: { Accept: 'application/json', 'User-Agent': 'Mozilla/5.0' } });
```

<!-- Current inline fetch call to remove (products/sync-anvisa/route.ts, ~line 71) — this
is a self-referential loopback call to the app's OWN /api/products endpoint, forwarding
the request's cookie header, paginated in a while loop. It is NOT a call to ANVISA's
external API (src/lib/anvisa-api.ts's fetchAnvisaData is unused in this file). -->
```typescript
const origin = new URL(req.url).origin;
const cookieHeader = req.headers.get('cookie') || '';
while (page <= totalPages) {
  const url = new URL('/api/products', origin);
  url.searchParams.set('page', String(page));
  // ...sets limit/sort/order/issuedNfeLookup/anvisaLookup...
  const response = await fetch(url.toString(), { headers: cookieHeader ? { cookie: cookieHeader } : undefined, cache: 'no-store' });
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create siscomex-client.ts and reroute ncm/bulk-sync</name>
  <files>src/lib/siscomex-client.ts, src/app/api/ncm/bulk-sync/route.ts</files>
  <read_first>
    src/app/api/ncm/bulk-sync/route.ts (full file — fetch block at lines ~50-83)
    src/lib/sefaz-client.ts (lines 1-50 — pattern reference for a dedicated external-integration client module: typed interfaces, class or function export, timeout handling)
    src/lib/nsdocs-client.ts (pattern reference for fetch-based client + custom error classes)
  </read_first>
  <action>
    Create `src/lib/siscomex-client.ts` exporting: `export interface SiscomexItem { Codigo?: string; codigo?: string; Descricao?: string; descricao?: string }`
    (moved from being route-local), `export class SiscomexApiError extends Error { constructor(public status: number) { super(\`SISCOMEX retornou status ${'$'}{status}\`); } }`,
    `export class SiscomexTimeoutError extends Error {}`, and
    `export async function fetchSiscomexNomenclature(): Promise<SiscomexItem[]>` that moves the
    exact current logic from `ncm/bulk-sync/route.ts` (const `SISCOMEX_URL`, 60s
    `AbortController` timeout, `Accept: application/json` + `User-Agent: Mozilla/5.0` headers,
    `res.ok` check throwing `SiscomexApiError` with the status instead of returning a
    `NextResponse`, `AbortError` caught and rethrown as `SiscomexTimeoutError`, and the
    `json?.Nomenclaturas ?? json ?? []` / `Array.isArray` shape normalization — throw a plain
    `Error('Formato inesperado da resposta do SISCOMEX')` if not an array). Update
    `ncm/bulk-sync/route.ts` to import `fetchSiscomexNomenclature, SiscomexApiError, SiscomexTimeoutError`
    from `@/lib/siscomex-client`, call `const rawItems = await fetchSiscomexNomenclature();`
    inside the existing try/catch, and catch `SiscomexApiError`/`SiscomexTimeoutError`/generic
    `Error` to reproduce the exact same `502`/`504`/rethrow behavior and JSON error bodies
    the route currently returns inline. Keep `cleanDescription`, `parentCodeFor`, and the
    NCM hierarchy-building / batch-insert logic in the route unchanged — only the raw
    fetch is extracted.
  </action>
  <acceptance_criteria>
    `test -f src/lib/siscomex-client.ts &amp;&amp; echo OK` prints `OK`.
    `grep -c "export async function fetchSiscomexNomenclature" src/lib/siscomex-client.ts` returns `1`.
    `grep -c "fetch(SISCOMEX_URL" src/app/api/ncm/bulk-sync/route.ts` returns `0`.
    `grep -c "from '@/lib/siscomex-client'" src/app/api/ncm/bulk-sync/route.ts` returns `1`.
    `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>siscomex-client.ts exists with fetchSiscomexNomenclature(); ncm/bulk-sync/route.ts calls it instead of an inline fetch(), same response shapes preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Replace sync-anvisa's self-fetch loop with a direct call to buildProductsListPayload</name>
  <files>src/app/api/products/sync-anvisa/route.ts</files>
  <read_first>
    src/app/api/products/sync-anvisa/route.ts (full file — the `while (page <= totalPages)` fetch loop, lines ~46-84)
    src/lib/product-aggregation.ts (post-Plan-12-01: buildProductsListPayload signature + ProductsListQueryParams, moved here per Next.js route-export constraint)
  </read_first>
  <action>
    Replace the `origin`/`cookieHeader`/`while (page <= totalPages) { ... fetch(url.toString()) ... }`
    block with a single call:
    `import { buildProductsListPayload, type ProductsListQueryParams } from '@/lib/product-aggregation';`
    then
    `const payload = await buildProductsListPayload(company.id, { page: 1, limit: 200, search: '', sort: 'quantity', order: 'desc', useAnvisaLookup: true, useIssuedNfeLookup: true, onlyMissingAnvisa: false, exportAll: true, dateFrom: null, dateTo: null });`
    (`exportAll: true` returns every matching product in one call — the same overall
    result set the previous pagination loop accumulated across pages — set
    `sort`/`order`/`useIssuedNfeLookup`/`useAnvisaLookup` to the exact values the old
    loop passed as query params: `sort=quantity&order=desc&issuedNfeLookup=1&anvisaLookup=1`).
    Read `payload.products` directly into the existing `products: SyncProductItem[]`
    array (drop the `pageProducts`/`totalPages` accumulation variables entirely — no
    longer needed). Remove the now-unused `origin`/`cookieHeader` variables. Do not
    change anything below this block (the manual/catalog/registry-merge logic that
    consumes `products` stays identical).
  </action>
  <acceptance_criteria>
    `grep -c "await fetch(url.toString()" src/app/api/products/sync-anvisa/route.ts` returns `0`.
    `grep -c "buildProductsListPayload" src/app/api/products/sync-anvisa/route.ts` returns `>= 1`.
    `grep -c "cookieHeader" src/app/api/products/sync-anvisa/route.ts` returns `0`.
    `npm run build` exits 0.
    `npm run lint` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm run lint</automated>
  </verify>
  <done>sync-anvisa/route.ts calls buildProductsListPayload() in-process; zero raw fetch() calls remain in either route handler touched by this plan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke test of Phase 12 changes in dev</name>
  <what-built>
    Phase 12 complete: products/route.ts dedup'd against product-aggregation.ts
    with a byte-identical-response snapshot test (12-01); auto-sync.ts split into
    sync-scheduler.ts + sync-strategies/{sefaz,nsdocs,receita-nfse}.ts with a
    common SyncStrategy contract (12-02); siscomex-client.ts created and
    ncm/bulk-sync rerouted through it; products/sync-anvisa's self-fetch loop
    replaced with a direct in-process call (12-03).
  </what-built>
  <files>none (verification-only task, no files modified)</files>
  <action>
    No code changes in this task — perform the manual verification steps below
    against the dev server to confirm all 3 plans in Phase 12 behave correctly
    end-to-end before the phase is considered done.
  </action>
  <how-to-verify>
    1. Ensure the dev server is running (`qldev` or `PORT=3001 npm run dev`) and
       check the startup log for `"Scheduler iniciado - verificando a cada 60s"`
       (proves sync-scheduler.ts's startAutoSync still boots correctly from
       bootstrap.ts's dynamic import).
    2. Log into http://localhost:3001 (or the Tailscale dev URL), open the
       Produtos page — confirm the product list still loads normally (sanity
       check that nothing broke in shared modules).
    3. From Configurações/Sistema (or wherever the NCM sync action lives),
       trigger "Sincronizar NCM" (calls POST /api/ncm/bulk-sync) — confirm it
       returns `{ ok: true, total, inserted, updated }` and `ncm_cache` row
       count changes as expected.
    4. Trigger a "Sincronizar ANVISA" action on the Produtos page (calls POST
       /api/products/sync-anvisa) — confirm it completes without error and the
       returned `stats` (processed/updated/unchanged/manualSkipped) look
       reasonable compared to a run before this phase's changes.
    5. Trigger a manual sync (POST /api/nsdocs/sync, whichever method your test
       company has configured — SEFAZ, NSDocs, or Receita NFS-e) from the UI —
       confirm it still starts, and `GET /api/nsdocs/sync?syncLogId=...` still
       reports progress/completion as before.
  </how-to-verify>
  <verify>
    <human-check>All 5 steps above confirmed working by the developer in the dev environment.</human-check>
  </verify>
  <done>Developer replies "approved" (or issues are described and triaged before Phase 12 is marked complete).</done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| qlmed-app → SISCOMEX (portalunico.siscomex.gov.br, external Brazilian government API) | Existing boundary, now wrapped in a named client instead of inline in a handler |
| products/sync-anvisa handler → (removed) internal loopback to /api/products | Boundary eliminated by this plan — no longer forwards the request's session cookie internally, reducing attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | SISCOMEX response (external, unauthenticated government API) could return malformed/unexpected JSON | mitigate | fetchSiscomexNomenclature validates `Array.isArray` before returning; ncm/bulk-sync keeps its existing per-item `code.length < 4` guard and parameterized `$executeRawUnsafe` values (no string interpolation of untrusted data into SQL) |
| T-12-06 | Information Disclosure | Previous self-fetch forwarded the caller's session cookie to an internal /api/products call — removing it eliminates an unnecessary cookie relay | accept (improvement) | No mitigation needed — Task 2 removes the cookie-forwarding code path entirely |
| T-12-07 | Denial of Service | SISCOMEX client has a 60s timeout; a hung/slow SISCOMEX response could still tie up a request handler for up to 60s | accept | Matches existing behavior (same 60s timeout as before this refactor), out of scope to change here |
</threat_model>

<verification>
`npm run build` and `npm run lint` exit 0 after both tasks; zero raw `fetch()` calls remain in `ncm/bulk-sync/route.ts` or `sync-anvisa/route.ts`; human-verify checkpoint confirms end-to-end scheduler + manual sync + NCM sync + ANVISA sync all still work in the dev environment.
</verification>

<success_criteria>
- `siscomex-client.ts` exists and `ncm/bulk-sync` uses it exclusively for the SISCOMEX HTTP call.
- `products/sync-anvisa` no longer performs any raw `fetch()` call — it calls `buildProductsListPayload()` directly.
- Both routes' response shapes/status codes are unchanged from before this plan.
- Human-verify checkpoint passed, closing out Phase 12.
</success_criteria>

<output>
Create `.planning/phases/12-desduplica-o-de-c-digo/12-03-SUMMARY.md` when done
</output>
