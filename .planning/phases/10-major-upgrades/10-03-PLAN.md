---
phase: 10-major-upgrades
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - package.json
  - prisma/schema.prisma
  - src/lib/prisma.ts
autonomous: true
requirements:
  - UPG-03

must_haves:
  truths:
    - "Prisma 7 instalado (prisma e @prisma/client)"
    - "prisma generate executa sem erros"
    - "prisma db push executa sem drift warnings"
    - "Todas as raw queries ($queryRawUnsafe, $executeRawUnsafe) continuam funcionando"
    - "Build passa com Prisma 7"
  artifacts:
    - path: "package.json"
      provides: "Prisma 7 in dependencies"
    - path: "prisma/schema.prisma"
      provides: "Updated schema compatible with Prisma 7"
  key_links:
    - from: "src/lib/prisma.ts"
      to: "@prisma/client"
      via: "PrismaClient instantiation"
      pattern: "PrismaClient"
---

<objective>
Upgrade Prisma 5 to Prisma 7 (skipping Prisma 6).

Purpose: Prisma 5 is two major versions behind. Prisma 7 includes performance improvements, better TypeScript types, and security fixes. The codebase has 188 raw query calls across 37 files that need verification.

Output: Prisma 7 installed, schema compatible, all raw queries verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@prisma/schema.prisma
@src/lib/prisma.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Prisma 5 to Prisma 7</name>
  <files>package.json, prisma/schema.prisma, src/lib/prisma.ts</files>
  <action>
1. **Read Prisma 6 and 7 migration guides** before upgrading. Key areas:
   - Check https://www.prisma.io/docs/upgrade-guides for breaking changes
   - Schema syntax changes (if any)
   - Client API changes
   - Raw query API changes (critical: 188 raw queries in codebase)

2. **Upgrade Prisma**:
   ```bash
   npm install prisma@7 @prisma/client@7
   ```
   Use `--legacy-peer-deps` if needed.

3. **Run prisma generate**:
   ```bash
   npx prisma generate
   ```
   Fix any schema-level errors.

4. **Check schema compatibility** — Prisma 7 may:
   - Require new `generator` options
   - Change `@@ignore` handling (critical: we have 6+ @@ignore stubs)
   - Change `@@map` behavior
   - Require `previewFeatures` updates
   
   Fix any issues in `prisma/schema.prisma`.

5. **Check PrismaClient instantiation** — `src/lib/prisma.ts` may need updates:
   - Global singleton pattern may have new options
   - Log configuration may change
   - Connection pool settings

6. **Verify raw queries compile** — The biggest risk is 188 raw query calls. Key concerns:
   - `$queryRawUnsafe` and `$executeRawUnsafe` API stability
   - Return type changes (BigInt handling, Date formatting)
   - Parameter passing syntax
   
   Run `npx tsc --noEmit` and fix any type errors in raw query call sites.

7. **Run prisma db push** (CAREFULLY — shared DB):
   ```bash
   npx prisma db push
   ```
   This should be a no-op if schema hasn't changed structurally. Verify no unintended migrations.

8. **Build test**:
   ```bash
   npm run build
   ```
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx prisma generate 2>&1 | tail -5 && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Prisma 7 installed. prisma generate succeeds. TypeScript compiles. All raw queries compatible. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke test critical raw query paths</name>
  <files></files>
  <action>
Start dev server and verify the most critical raw-query-heavy endpoints work:

1. **Start dev server**: `PORT=3001 npm run dev`

2. **Test products/list** (3 raw queries):
   ```bash
   curl -s http://localhost:3001/api/products/list -H 'Cookie: ...' | head -c 200
   ```

3. **Test health endpoint** (1 raw query):
   ```bash
   curl -s http://localhost:3001/api/health | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('status','FAIL'))"
   ```

4. **Test fiscal dashboard** (4 raw queries):
   ```bash
   curl -s http://localhost:3001/api/fiscal/dashboard -H 'Cookie: ...' | head -c 200
   ```

5. If any endpoint fails with Prisma errors, check the error message for:
   - Type mismatches (BigInt vs number)
   - Parameter binding changes
   - Missing client generation

Fix issues in the relevant files.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Critical endpoints with raw queries respond correctly. No Prisma runtime errors.</done>
</task>

</tasks>

<verification>
- `npx prisma --version` shows 7.x
- `npx prisma generate` succeeds
- `npx tsc --noEmit` passes
- `npm run build` passes
- `curl http://localhost:3001/api/health` returns status ok
</verification>

<success_criteria>
- Prisma 7 (prisma + @prisma/client) installed
- Schema generates without errors
- 188 raw queries compile without type errors
- Critical endpoints work at runtime
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-major-upgrades/10-03-SUMMARY.md`
</output>
