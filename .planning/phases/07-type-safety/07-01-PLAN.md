---
phase: 07-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/nfe-xml.ts
  - src/types/cte-xml.ts
  - src/types/nfse-xml.ts
  - src/types/xml-common.ts
  - src/lib/xml-helpers.ts
autonomous: true
requirements:
  - TYPE-01

must_haves:
  truths:
    - "NFeXml interface describes the full fast-xml-parser output for NF-e documents"
    - "CTeXml interface describes the full fast-xml-parser output for CT-e documents"
    - "NFSeXml interface describes the full fast-xml-parser output for NFS-e documents"
    - "xml-helpers val/num/gv accept Record<string, unknown> instead of any"
  artifacts:
    - path: "src/types/nfe-xml.ts"
      provides: "NFeXml, NFeDet, NFeEmit, NFeDest, NFeProd, NFeImposto interfaces"
      min_lines: 80
    - path: "src/types/cte-xml.ts"
      provides: "CTeXml, CTeInfCte, CTeRem, CTeDest, CTeTomador interfaces"
      min_lines: 60
    - path: "src/types/nfse-xml.ts"
      provides: "NFSeXml, NFSeServico, NFSePrestador, NFSeTomador interfaces"
      min_lines: 40
    - path: "src/types/xml-common.ts"
      provides: "XmlNode, XmlValue shared types for parsed XML trees"
      min_lines: 10
    - path: "src/lib/xml-helpers.ts"
      provides: "val/num/gv with XmlNode parameter type"
      exports: ["val", "num", "gv"]
  key_links:
    - from: "src/types/nfe-xml.ts"
      to: "src/types/xml-common.ts"
      via: "import XmlNode"
      pattern: "import.*XmlNode.*xml-common"
    - from: "src/lib/xml-helpers.ts"
      to: "src/types/xml-common.ts"
      via: "import XmlNode"
      pattern: "import.*XmlNode.*xml-common"
---

<objective>
Create typed interfaces for all three XML document types parsed by fast-xml-parser in QLMED (NF-e, CT-e, NFS-e), plus a shared XmlNode base type. Then update xml-helpers.ts to use the new types instead of `any`.

Purpose: Establish the type contracts that all subsequent TYPE-02 and TYPE-03 work depends on. Without these interfaces, parsing functions cannot be typed.
Output: 4 new type files in src/types/ and updated xml-helpers.ts
</objective>

<execution_context>
@/home/marce/.claude/get-shit-done/workflows/execute-plan.md
@/home/marce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/marce/QLMED/dev/.planning/ROADMAP.md
@/home/marce/QLMED/dev/.planning/REQUIREMENTS.md

@/home/marce/QLMED/dev/src/types/index.ts
@/home/marce/QLMED/dev/src/types/invoice-details.ts
@/home/marce/QLMED/dev/src/lib/xml-helpers.ts
@/home/marce/QLMED/dev/src/lib/parse-invoice-xml.ts
@/home/marce/QLMED/dev/src/app/api/invoices/[id]/pdf/route.ts
@/home/marce/QLMED/dev/src/app/api/invoices/[id]/details/route.ts
@/home/marce/QLMED/dev/src/lib/product-aggregation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XML type interfaces and shared XmlNode type</name>
  <files>src/types/xml-common.ts, src/types/nfe-xml.ts, src/types/cte-xml.ts, src/types/nfse-xml.ts</files>
  <action>
    Derive interfaces from actual usage patterns in the codebase. Read the following files to understand the XML structure accessed:
    - src/lib/parse-invoice-xml.ts — shows top-level keys: nfeProc.NFe.infNFe, cteProc.CTe.infCte, CompNfse.Nfse
    - src/app/api/invoices/[id]/pdf/route.ts — shows detailed field access for PDF generation (NFe det, emit, dest, total, transp, cobr, infAdFisco, infCpl, ide, etc.)
    - src/app/api/invoices/[id]/details/route.ts — shows same structures for detail display
    - src/lib/product-aggregation.ts — shows det.prod fields, rastro, ANVISA med

    **xml-common.ts**: Create base types:
    ```typescript
    export type XmlValue = string | number | undefined;
    export type XmlNode = Record<string, unknown>;
    ```

    **nfe-xml.ts**: Create interfaces covering the NFe parsed tree. Key structures:
    - NFeProc (top-level wrapper with protNFe)
    - NFeDoc (NFe element with infNFe)
    - NFeInfNFe (ide, emit, dest, det[], total, transp, cobr, pag, infAdFisco, infCpl)
    - NFeIde (cUF, cNF, natOp, mod, serie, nNF, dhEmi, tpNF, idDest, cMunFG, tpImp, tpEmis, cDV, tpAmb, finNFe, indFinal, indPres, procEmi, verProc)
    - NFeEmit / NFeDest (CNPJ/CPF, xNome, xFant, IE, IEST, IM, CNAE, CRT, enderEmit/enderDest with xLgr, nro, xCpl, xBairro, cMun, xMun, UF, CEP, cPais, xPais, fone, email)
    - NFeDet (nItem attr, prod, imposto)
    - NFeProd (cProd, cEAN, xProd, NCM, CEST, CFOP, uCom, qCom, vUnCom, vProd, cEANTrib, uTrib, qTrib, vUnTrib, vFrete, vSeg, vDesc, vOutro, indTot, rastro[], med/ANVISA)
    - NFeImposto (ICMS, IPI, PIS, COFINS with nested CST groups)
    - NFeTotal (ICMSTot with vBC, vICMS, vICMSDeson, vFCPUFDest, vICMSUFDest, vICMSUFRemet, vFCP, vBCST, vST, vFCPST, vFCPSTRet, vProd, vFrete, vSeg, vDesc, vII, vIPI, vIPIDevol, vPIS, vCOFINS, vOutro, vNF, vTotTrib)
    - NFeTransp, NFeVol, NFeCobr, NFeDup, NFePag

    Use `XmlNode` for deeply nested or rarely-accessed sub-structures. Use specific interfaces for frequently accessed paths. All leaf values should be `string | undefined` (fast-xml-parser returns strings). Mark all properties as optional (`?:`) since XML elements may be absent.

    **cte-xml.ts**: Create interfaces for CT-e:
    - CTeProc (protCTe, CTe.infCte)
    - CTeInfCte (ide, compl, emit, rem, dest, exped, receb, tomador, vPrest, imp, infCTeNorm with infCarga, infDoc, infModal)
    - CTeIde, CTeEmit, CTeRem, CTeDest, CTeExped, CTeReceb (with ender sub-objects)
    - CTeVPrest (vTPrest, vRec, Comp[])
    - CTeInfCarga (vCarga, proPred, xOutCat, infQ[])
    - CTeCompl (xObs, ObsCont[], ObsFisco[])

    **nfse-xml.ts**: Create interfaces for NFS-e (both Nacional and ABRASF formats):
    - NFSeCompNfse (Nfse with InfNfse)
    - NFSeInfNfse (Numero, CodigoVerificacao, DataEmissao, Servico, PrestadorServico, TomadorServico, ValoresNfse)
    - NFSeServico (Valores, ItemListaServico, CodigoTributacaoMunicipio, Discriminacao, MunicipioPrestacaoServico)
    - NFSeValores (ValorServicos, ValorDeducoes, ValorPis, ValorCofins, ValorInss, ValorIr, ValorCsll, ValorIss, Aliquota, BaseCalculo)
    - NFSePrestador, NFSeTomador (IdentificacaoPrestador/Tomador, RazaoSocial, Endereco)

    All properties optional. Use `string | undefined` for leaf nodes. Use `T | T[]` pattern where ensureArray is commonly called. Export all interfaces.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit src/types/xml-common.ts src/types/nfe-xml.ts src/types/cte-xml.ts src/types/nfse-xml.ts 2>&1 | head -20</automated>
  </verify>
  <done>All 4 type files exist, compile without errors, and export interfaces matching the actual XML structures used across parse-invoice-xml.ts, pdf/route.ts, details/route.ts, and product-aggregation.ts</done>
</task>

<task type="auto">
  <name>Task 2: Type xml-helpers.ts with XmlNode</name>
  <files>src/lib/xml-helpers.ts</files>
  <action>
    Update val(), num(), and gv() in xml-helpers.ts to use `XmlNode` from `@/types/xml-common` instead of `any`:

    ```typescript
    import type { XmlNode } from '@/types/xml-common';

    export function val(obj: XmlNode | null | undefined, ...keys: string[]): string { ... }
    export function num(obj: XmlNode | null | undefined, key: string): number { ... }
    export function gv(obj: XmlNode | null | undefined, ...keys: string[]): string { ... }
    ```

    The internal logic stays identical — only the parameter type annotation changes. Since XmlNode is `Record<string, unknown>`, the `obj?.[k]` access pattern still works. The `cur` variable in gv() should be typed as `unknown` during traversal.

    After updating, run tsc to confirm no downstream breakage. If any caller passes a more specific type that is not assignable to XmlNode, add the import and ensure the specific interface extends or is compatible with XmlNode.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>xml-helpers.ts has zero `any` annotations. val/num/gv accept XmlNode. Full project compiles without errors.</done>
</task>

</tasks>

<verification>
- `grep ': any' src/types/nfe-xml.ts src/types/cte-xml.ts src/types/nfse-xml.ts src/types/xml-common.ts src/lib/xml-helpers.ts` returns zero results
- `npx tsc --noEmit` passes (no new type errors introduced)
- All 4 type files export their interfaces
</verification>

<success_criteria>
- NFeXml, CTeXml, NFSeXml interfaces exist and accurately model the XML structures accessed in the codebase
- xml-helpers.ts uses XmlNode instead of any (3 any eliminated)
- Full project compiles with tsc --noEmit
</success_criteria>

<output>
After completion, create `.planning/phases/07-type-safety/07-01-SUMMARY.md`
</output>
