---
phase: 07-type-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/lib/parse-invoice-xml.ts
  - src/app/api/invoices/[id]/pdf/route.ts
  - src/app/api/invoices/[id]/details/route.ts
  - src/lib/product-aggregation.ts
autonomous: true
requirements:
  - TYPE-02

must_haves:
  truths:
    - "parse-invoice-xml.ts functions receive typed XML objects, not any"
    - "PDF route helper functions (getParty, normalizeCteParty, extractCteData, extractDanfeData, extractNfseData) use XML interfaces"
    - "details/route.ts parsing functions use typed parameters"
    - "product-aggregation.ts extractBatches and extractAnvisa use NFeDet/NFeProd types"
  artifacts:
    - path: "src/lib/parse-invoice-xml.ts"
      provides: "Typed parsing functions for NF-e, CT-e, NFS-e"
      contains: "NFeProc|CTeProc|NFSeCompNfse"
    - path: "src/app/api/invoices/[id]/pdf/route.ts"
      provides: "Typed PDF generation helpers"
      contains: "NFeInfNFe|CTeInfCte|NFSeInfNfse"
    - path: "src/app/api/invoices/[id]/details/route.ts"
      provides: "Typed detail extraction functions"
      contains: "NFeInfNFe|CTeInfCte"
    - path: "src/lib/product-aggregation.ts"
      provides: "Typed product extraction from XML det nodes"
      contains: "NFeDet|NFeProd"
  key_links:
    - from: "src/lib/parse-invoice-xml.ts"
      to: "src/types/nfe-xml.ts"
      via: "import NFeProc"
      pattern: "import.*NFeProc.*nfe-xml"
    - from: "src/app/api/invoices/[id]/pdf/route.ts"
      to: "src/types/nfe-xml.ts"
      via: "import XML types"
      pattern: "import.*nfe-xml"
    - from: "src/app/api/invoices/[id]/details/route.ts"
      to: "src/types/nfe-xml.ts"
      via: "import XML types"
      pattern: "import.*nfe-xml"
---

<objective>
Apply the XML type interfaces created in Plan 01 to all core parsing functions — eliminating ~46 uses of `any` in parse-invoice-xml.ts (6), pdf/route.ts (16), details/route.ts (19), and product-aggregation.ts (2). These are the primary XML-handling functions.

Purpose: Make XML parsing type-safe so TypeScript catches field access errors at compile time instead of runtime.
Output: 4 files updated with zero `any` in XML parameter positions.
</objective>

<execution_context>
@/home/marce/.claude/get-shit-done/workflows/execute-plan.md
@/home/marce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/marce/QLMED/dev/.planning/ROADMAP.md
@/home/marce/QLMED/dev/.planning/REQUIREMENTS.md
@/home/marce/QLMED/dev/.planning/phases/07-type-safety/07-01-SUMMARY.md

@/home/marce/QLMED/dev/src/types/nfe-xml.ts
@/home/marce/QLMED/dev/src/types/cte-xml.ts
@/home/marce/QLMED/dev/src/types/nfse-xml.ts
@/home/marce/QLMED/dev/src/types/xml-common.ts
@/home/marce/QLMED/dev/src/lib/xml-helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type parse-invoice-xml.ts and product-aggregation.ts</name>
  <files>src/lib/parse-invoice-xml.ts, src/lib/product-aggregation.ts</files>
  <action>
    **parse-invoice-xml.ts** (6 any to fix):
    Import types from @/types/nfe-xml, @/types/cte-xml, @/types/nfse-xml, @/types/xml-common.

    Replace `any` parameter types in these functions:
    - `extractAccessKey(proc: any, inf: any, ...)` → use XmlNode for both (these are generic wrappers)
    - `getPartyInfo(node: any)` → `getPartyInfo(node: XmlNode)` (accesses CNPJ/CPF, xNome, xFant, IE, enderEmit/enderDest)
    - `extractCteTomador(infCte: any)` → `extractCteTomador(infCte: CTeInfCte)`
    - `parseNFe(result: any)` → `parseNFe(result: { nfeProc?: NFeProc; NFe?: NFeDoc })`
    - `parseCTe(result: any)` → `parseCTe(result: { cteProc?: CTeProc; CTe?: CTeDoc })`
    - `parseNFSe(result: any)` → `parseNFSe(result: { CompNfse?: NFSeCompNfse; ... })`

    For NFS-e, check the actual parsing path in the file — it may use Nacional or ABRASF format. Type accordingly.

    **product-aggregation.ts** (2 any to fix):
    - `extractBatches(det: any, prod: any)` → `extractBatches(det: NFeDet, prod: NFeProd)`
    - `extractAnvisa(det: any, prod: any)` → `extractAnvisa(det: NFeDet, prod: NFeProd)`

    Import NFeDet/NFeProd from @/types/nfe-xml.

    If the XML interfaces from Plan 01 are missing fields that these functions access, add the missing fields to the interfaces (they may have been missed during interface creation). Keep all added fields optional.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>parse-invoice-xml.ts and product-aggregation.ts have zero `any`. All 8 functions use concrete XML types. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Type pdf/route.ts and details/route.ts parsing functions</name>
  <files>src/app/api/invoices/[id]/pdf/route.ts, src/app/api/invoices/[id]/details/route.ts</files>
  <action>
    **pdf/route.ts** (16 any to fix):
    Import types from @/types/nfe-xml, @/types/cte-xml, @/types/nfse-xml, @/types/xml-common.

    Replace `any` in these functions:
    - `getParty(node: any)` → `getParty(node: XmlNode)` — generic party extractor
    - `parseCteTomador(infCte: any)` → `parseCteTomador(infCte: CTeInfCte)`
    - `normalizeCteParty(node: any, ender: any)` → `normalizeCteParty(node: XmlNode, ender: XmlNode)`
    - `extractCteData(parsed: any, ...)` → `extractCteData(parsed: CTeProc | { CTe: CTeDoc }, ...)`
    - `extractDanfeData(parsed: any)` → `extractDanfeData(parsed: NFeProc | { NFe: NFeDoc })`
    - `extractIcmsFromImposto(imp: any)` → `extractIcmsFromImposto(imp: XmlNode)` (accesses ICMS sub-groups)
    - `extractIpiFromImposto(imp: any)` → `extractIpiFromImposto(imp: XmlNode)`
    - `normalizeNfseParty(node: any, enderNode: any)` → `normalizeNfseParty(node: XmlNode, enderNode: XmlNode)`
    - `extractNfseData(parsed: any, ...)` → use NFSeCompNfse type

    For inline callback `any` casts (lines like `dets.map((det: any) =>`, `dups.map((d: any) =>`):
    - Type the arrays properly: `const dets: NFeDet[] = ensureArray(infNFe?.det);`
    - Then `.map((det) => ...)` infers the type without explicit annotation

    Similarly for `(item: any)` in Comp[], infQ[], etc. — type the source array.

    **details/route.ts** (19 any to fix):
    Import types from @/types/nfe-xml, @/types/cte-xml, @/types/nfse-xml, @/types/xml-common.

    Replace `any` in:
    - `parseEmitDest(node: any)` → `parseEmitDest(node: XmlNode)`
    - `parseProdutos(det: any)` → `parseProdutos(det: NFeDet | NFeDet[])` + fix `items.map((item: any, ...) =>` by typing the array
    - `parseTotais(total: any)` → `parseTotais(total: XmlNode)`
    - `parseTransporte(transp: any)` → `parseTransporte(transp: XmlNode)` + fix `volumes.map((v: any) =>`
    - `parseCobranca(cobr: any, pag: any)` → `parseCobranca(cobr: XmlNode, pag: XmlNode)` + fix `(p: any)` and `(d: any)` in map callbacks
    - `parseInfAdicionais(infAdFisco: any, infCpl: any, ide: any)` → use XmlNode for all three
    - `parseCteParty(node: any)` → `parseCteParty(node: XmlNode)`
    - `parseCteDetails(invoice: any, infCte: any, cteProc: any)` → type invoice as PrismaInvoice or the view type used, infCte as CTeInfCte, cteProc as CTeProc. Fix inline `(q: any)`, `(n: any)`, `(o: any)`, `(c: any)` map callbacks by typing source arrays.
    - `parseNfseParty(node: any, ...)` → `parseNfseParty(node: XmlNode, ...)`
    - `parseNfseDetails(invoice: any, nacional: any, abrasf: any)` → type nacional/abrasf with NFSe types, invoice with its DB type

    For the `response: any` and `metaResponse: any` object literals: define inline types or use existing invoice-details.ts types if they match. At minimum replace with `Record<string, unknown>` if a specific interface is impractical.

    After all changes, run tsc to verify. If new type errors surface from accessing properties that don't exist on the typed interfaces, either:
    1. Add the missing property to the XML interface (preferred)
    2. Use a narrowing check: `if ('prop' in node) { ... }`
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>pdf/route.ts and details/route.ts have zero `any` in function signatures and map callbacks. All XML access is through typed interfaces. Project compiles.</done>
</task>

</tasks>

<verification>
- `grep -c ': any' src/lib/parse-invoice-xml.ts src/app/api/invoices/[id]/pdf/route.ts src/app/api/invoices/[id]/details/route.ts src/lib/product-aggregation.ts` — all return 0
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All 4 core XML processing files have zero `any` annotations
- ~46 `any` occurrences eliminated
- XML interfaces from Plan 01 are imported and used consistently
- Full project compiles without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-type-safety/07-02-SUMMARY.md`
</output>
