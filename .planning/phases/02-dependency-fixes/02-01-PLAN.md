---
phase: 02-dependency-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/certificate-manager.ts
  - src/app/api/reports/valvulas-importadas/pdf/route.ts
autonomous: true
requirements: [DEP-01, DEP-02, DEP-03, DEP-05]

must_haves:
  truths:
    - "npm audit shows zero high/critical vulnerabilities in transitive deps"
    - "Certificate A1 upload and PFX parsing still works with updated node-forge"
    - "Email sending via nodemailer v8 still works (createTransport + sendMail API)"
    - "html-to-image is no longer in package.json"
  artifacts:
    - path: "package.json"
      provides: "Updated dependencies with node-forge latest, nodemailer v8, no html-to-image"
      contains: "nodemailer"
    - path: "src/lib/certificate-manager.ts"
      provides: "PFX certificate processing using node-forge"
      contains: "forge.pkcs12"
    - path: "src/app/api/reports/valvulas-importadas/pdf/route.ts"
      provides: "Email sending using nodemailer"
      contains: "nodemailer"
  key_links:
    - from: "src/lib/certificate-manager.ts"
      to: "node-forge"
      via: "import forge from 'node-forge'"
      pattern: "forge\\.asn1|forge\\.pkcs12|forge\\.pki"
    - from: "src/app/api/reports/valvulas-importadas/pdf/route.ts"
      to: "nodemailer"
      via: "import nodemailer from 'nodemailer'"
      pattern: "nodemailer\\.createTransport|transporter\\.sendMail"
---

<objective>
Update all dependency versions and remove unused packages to eliminate known CVEs.

Purpose: Fix transitive vulnerabilities (npm audit fix), update node-forge (4 CVEs including signature forgery), update nodemailer to v8 (SMTP injection), and remove unused html-to-image.
Output: Clean dependency tree with zero known high/critical CVEs, all existing functionality preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/lib/certificate-manager.ts
@src/app/api/reports/valvulas-importadas/pdf/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: npm audit fix + remove html-to-image + update node-forge</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json</read_first>
  <action>
    Run the following npm commands in sequence:

    1. `npm audit fix` — fixes transitive vulns in basic-ftp, @xmldom/xmldom, flatted, vite, ajv, brace-expansion, minimatch (DEP-01)
    2. `npm uninstall html-to-image` — zero imports in src/, confirmed dead dependency (DEP-05)
    3. `npm install node-forge@latest` — update from 1.3.3 to latest, fixes 4 CVEs including signature forgery CVE (DEP-02)
    4. `npm install nodemailer@8 @types/nodemailer@8` — update from v7 to v8, fixes SMTP injection via CRLF (DEP-03). Note: @types/nodemailer is currently v7.0.11 in dependencies (wrong section — it's a type package). Move it to devDependencies if needed, but the priority is getting v8.

    After all installs, run `npm audit` to verify zero high/critical vulnerabilities remain.

    IMPORTANT: node-forge API for PFX/PKCS12 parsing has been stable across versions. The key functions used are:
    - `forge.asn1.fromDer()` — ASN.1 parsing
    - `forge.pkcs12.pkcs12FromAsn1()` — PKCS12 decryption
    - `forge.pki.oids.certBag` — OID constants
    These are core APIs unlikely to have breaking changes, but verify the build succeeds.

    IMPORTANT: nodemailer v8 API. The `createTransport()` and `sendMail()` APIs are stable across v7-v8. The main v8 change was fixing CRLF injection in SMTP commands. The current usage pattern (createTransport with host/port/auth, then sendMail with from/to/subject/text/attachments) is fully compatible with v8. No code changes expected.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npm audit 2>&1 | tail -5 && node -e "require('node-forge')" && node -e "require('nodemailer')" && ! grep -q html-to-image package.json && echo "ALL OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm audit` reports 0 high/critical vulnerabilities
    - `node -e "require('node-forge')"` exits 0
    - `node -e "require('nodemailer')"` exits 0
    - `grep -c html-to-image package.json` returns 0
    - `grep "node-forge" package.json` shows version newer than 1.3.3
    - `grep "nodemailer" package.json` shows version 8.x
  </acceptance_criteria>
  <done>All four dependency changes applied. npm audit clean. node-forge, nodemailer importable. html-to-image gone.</done>
</task>

<task type="auto">
  <name>Task 2: Verify node-forge and nodemailer usage compiles correctly</name>
  <files>src/lib/certificate-manager.ts, src/app/api/reports/valvulas-importadas/pdf/route.ts</files>
  <read_first>src/lib/certificate-manager.ts, src/app/api/reports/valvulas-importadas/pdf/route.ts</read_first>
  <action>
    1. Read `src/lib/certificate-manager.ts` and verify the node-forge API calls are compatible with the installed version. Key patterns to check:
       - `forge.asn1.fromDer(pfxDer)` — must accept binary string
       - `forge.pkcs12.pkcs12FromAsn1(p12Asn1, password)` — must return object with getBags()
       - `forge.pki.oids.certBag` — must be a valid OID string
       - cert.serialNumber, cert.issuer.attributes, cert.subject.attributes, cert.validity — must exist
       If any API changed, update the code to match new API.

    2. Read `src/app/api/reports/valvulas-importadas/pdf/route.ts` and verify nodemailer v8 compatibility:
       - `nodemailer.createTransport({host, port, secure, auth})` — standard, no changes expected
       - `transporter.sendMail({from, to, subject, text, attachments})` — standard, no changes expected
       If the import style changed (e.g., named exports instead of default), update accordingly.

    3. Run `npx tsc --noEmit --pretty 2>&1 | grep -E "(certificate-manager|valvulas)" | head -20` to check for type errors in these specific files.

    4. If there are TypeScript errors related to the updated packages, fix them:
       - For node-forge: check if `@types/node-forge` needs updating too (`npm install @types/node-forge@latest`)
       - For nodemailer: `@types/nodemailer@8` should already be installed from Task 1

    5. Run `npm run build` to verify the full application compiles with the updated dependencies.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - No TypeScript errors referencing certificate-manager.ts or nodemailer
    - `npx tsc --noEmit 2>&1 | grep -c "certificate-manager\|valvulas\|nodemailer\|node-forge"` returns 0
  </acceptance_criteria>
  <done>Application builds successfully with updated node-forge and nodemailer. No type errors in certificate-manager.ts or valvulas route. Build output confirms standalone server ready.</done>
</task>

</tasks>

<verification>
1. `npm audit` shows zero high/critical vulnerabilities
2. `npm run build` completes successfully
3. `grep -c html-to-image package.json` returns 0
4. node-forge version in package.json is newer than 1.3.3
5. nodemailer version in package.json is 8.x
</verification>

<success_criteria>
- Zero high/critical CVEs in npm audit output
- Application builds without errors
- html-to-image removed from package.json
- node-forge updated to latest (fixes 4 CVEs)
- nodemailer updated to v8 (fixes SMTP injection)
- Certificate manager and email sending code compile correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-dependency-fixes/02-01-SUMMARY.md`
</output>
