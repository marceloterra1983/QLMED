---
phase: 04-xml-extraction-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/products/route.ts
  - src/app/(painel)/cadastro/produtos/page-client.tsx
autonomous: true
requirements: [PERF-04]

must_haves:
  truths:
    - "Legacy /api/products route has a deprecation header and log warning"
    - "Export-all functionality (exportAll=1) in the frontend uses /api/products/list instead of the legacy route"
  artifacts:
    - path: "src/app/api/products/route.ts"
      provides: "Deprecation warning on legacy route"
      contains: "X-Deprecated"
    - path: "src/app/(painel)/cadastro/produtos/page-client.tsx"
      provides: "Frontend using /api/products/list for export"
      contains: "api/products/list"
  key_links:
    - from: "src/app/(painel)/cadastro/produtos/page-client.tsx"
      to: "/api/products/list"
      via: "fetch calls for export"
      pattern: "api/products/list.*exportAll"
---

<objective>
Deprecate the legacy /api/products route that loads up to 3000+ XMLs. Redirect frontend export calls to /api/products/list which uses the pre-aggregated product_registry table.

Purpose: The legacy route parses thousands of XML documents on every call. The /api/products/list route already serves the main product listing from product_registry. Only the export functionality (exportAll=1) and ANVISA sync still hit the legacy route.
Output: Legacy route marked deprecated with header, frontend export calls migrated to /api/products/list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@src/app/api/products/route.ts
@src/app/(painel)/cadastro/produtos/page-client.tsx
@src/app/api/products/list/route.ts

<interfaces>
<!-- Frontend calls to legacy /api/products (from page-client.tsx): -->
Line 975: fetch('/api/products?exportAll=1&sort=lastIssue&order=desc')  — CSV export
Line 1023: fetch('/api/products?exportAll=1&onlyMissingAnvisa=1&sort=lastIssue&order=desc') — ANVISA export
Line 1359: fetch('/api/products?exportAll=1&sort=lastIssue&order=desc') — auto-classify all

<!-- Frontend calls to /api/products/list (already working): -->
Line 712: fetch(`/api/products/list?${params}`)  — main product listing
Line 732: fetch(`/api/products/list?${params}`)  — refresh after rebuild

<!-- /api/products/list already supports: -->
- search, sort, order, page, limit, exportAll params
- Returns product data from product_registry table
- Much faster than XML parsing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deprecation header to legacy products route</name>
  <files>src/app/api/products/route.ts</files>
  <action>
At the very beginning of the GET handler (after auth check), add:

```typescript
console.warn('[DEPRECATED] /api/products called — use /api/products/list instead');
```

Before every `return NextResponse.json(...)` in the file, add the deprecation header:

```typescript
const response = NextResponse.json({ ... });
response.headers.set('X-Deprecated', 'Use /api/products/list instead');
return response;
```

Do NOT delete the route — it is still used by ANVISA sync features that depend on XML-parsed data (like `onlyMissingAnvisa`). Just mark it deprecated so it can be tracked and eventually removed.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && grep -c "X-Deprecated" src/app/api/products/route.ts</automated>
  </verify>
  <done>Legacy /api/products route returns X-Deprecated header on all responses. Console warning logged on each call.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate frontend export calls to /api/products/list</name>
  <files>src/app/(painel)/cadastro/produtos/page-client.tsx</files>
  <action>
Change the following fetch calls from `/api/products` to `/api/products/list`:

1. Line ~975 (handleExportCSV): Change `fetch('/api/products?exportAll=1&sort=lastIssue&order=desc')` to `fetch('/api/products/list?exportAll=1&sort=lastIssueDate&order=desc')`. Note: sort param is `lastIssueDate` in /list (not `lastIssue`).

2. Line ~1359 (auto-classify all products fetch): Change `fetch('/api/products?exportAll=1&sort=lastIssue&order=desc')` to `fetch('/api/products/list?exportAll=1&sort=lastIssueDate&order=desc')`.

3. Line ~1023 (ANVISA missing export): This one uses `onlyMissingAnvisa=1` which /api/products/list should also support. Change to `fetch('/api/products/list?exportAll=1&onlyMissingAnvisa=1&sort=lastIssueDate&order=desc')`. If /api/products/list does NOT support `onlyMissingAnvisa`, leave this call pointing to legacy route — it requires XML data.

IMPORTANT: Check if /api/products/list supports `exportAll` and `onlyMissingAnvisa` params before changing. If it does not support them, leave those specific calls on the legacy route and add a TODO comment: `// TODO: migrate to /api/products/list when it supports exportAll/onlyMissingAnvisa`.

The response shape may differ between routes. After changing the URL, verify that the response properties accessed (`.products`, `.pagination`, etc.) match what /api/products/list returns. Adjust property names if needed.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && grep -n "api/products?" src/app/\(painel\)/cadastro/produtos/page-client.tsx | grep -v "api/products/list\|api/products/bulk\|api/products/rename\|api/products/details\|api/products/history\|api/products/settings\|api/products/sync\|api/products/anvisa\|api/products/rebuild\|api/products/import\|api/products/auto"</automated>
  </verify>
  <done>Frontend export calls use /api/products/list where possible. Any remaining legacy calls have TODO comments explaining why they cannot be migrated yet. The main listing was already on /api/products/list.</done>
</task>

</tasks>

<verification>
1. Legacy route has X-Deprecated header on responses
2. Frontend CSV export and auto-classify calls point to /api/products/list
3. No TypeScript errors in page-client.tsx
4. `grep -c "api/products?" src/app/\(painel\)/cadastro/produtos/page-client.tsx` shows minimal remaining legacy calls
</verification>

<success_criteria>
- /api/products returns X-Deprecated header
- Frontend uses /api/products/list for standard export operations
- ANVISA-specific calls that need XML data remain on legacy route with deprecation TODO
- No functional regression in product export
</success_criteria>

<output>
After completion, create `.planning/phases/04-xml-extraction-performance/04-03-SUMMARY.md`
</output>
