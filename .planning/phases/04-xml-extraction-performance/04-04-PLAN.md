---
phase: 04-xml-extraction-performance
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/lib/contact-fiscal-store.ts
  - src/app/api/customers/route.ts
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "contact_fiscal table has a city column storing xMun - UF extracted from XML"
    - "Customers route reads city from contact_fiscal instead of loading xmlContent"
    - "Existing invoices have city data backfilled in contact_fiscal"
  artifacts:
    - path: "src/lib/contact-fiscal-store.ts"
      provides: "city column in contact_fiscal table + upsert with city"
      contains: "city"
    - path: "src/app/api/customers/route.ts"
      provides: "City lookup from contact_fiscal instead of XML parsing"
      contains: "contact_fiscal"
  key_links:
    - from: "src/app/api/customers/route.ts"
      to: "contact_fiscal table"
      via: "raw SQL query for city by CNPJ"
      pattern: "contact_fiscal.*city"
    - from: "src/lib/contact-fiscal-store.ts"
      to: "contact_fiscal table"
      via: "ALTER TABLE ADD COLUMN city"
      pattern: "ADD COLUMN.*city"
---

<objective>
Add city (xMun + UF) to the contact_fiscal table and use it in customers route instead of loading xmlContent at runtime.

Purpose: Currently, customers/route.ts loads the full xmlContent for one invoice per customer just to regex-extract the city name. The contact_fiscal table already stores fiscal data per CNPJ — adding a city column is the minimal change. A one-time backfill script populates existing data.
Output: contact_fiscal table with city column, customers route reading from it, backfill for existing data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@src/lib/contact-fiscal-store.ts
@src/app/api/customers/route.ts

<interfaces>
<!-- contact_fiscal table (from contact-fiscal-store.ts): -->
CREATE TABLE IF NOT EXISTS contact_fiscal (
  id TEXT PRIMARY KEY,
  company_id TEXT NOT NULL,
  cnpj TEXT NOT NULL,
  ie TEXT, im TEXT, crt TEXT, uf TEXT,
  source_invoice_id TEXT,
  extracted_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(company_id, cnpj)
)

<!-- ContactFiscalRow interface: -->
export interface ContactFiscalRow {
  id: string; companyId: string; cnpj: string;
  ie: string | null; im: string | null; crt: string | null;
  uf: string | null; sourceInvoiceId: string | null; extractedAt: Date;
}

<!-- Current city extraction in customers/route.ts (lines 151-173): -->
Queries DISTINCT ON (recipientCnpj) with xmlContent, then regex-extracts
<enderDest><xMun> and <UF> to build "xMun - UF" string.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add city column to contact_fiscal table and update upsert</name>
  <files>src/lib/contact-fiscal-store.ts</files>
  <action>
1. In `ensureContactFiscalTable()`, after the CREATE TABLE statement, add an ALTER TABLE to add the city column if not exists:
```sql
ALTER TABLE contact_fiscal ADD COLUMN IF NOT EXISTS city TEXT;
```

2. Update `ContactFiscalRow` interface to include `city: string | null`.
3. Update `ContactFiscalDbRow` interface to include `city: string | null`.
4. Find the upsert function (likely `upsertContactFiscal` or similar) and add `city` to the INSERT and ON CONFLICT UPDATE statements. If the upsert function exists, add city as an optional parameter. If it does not exist, check how contact_fiscal rows are created and add city to that flow.
5. Update the row mapping function to include city.

CRITICAL: Use `ALTER TABLE IF NOT EXISTS` pattern (ADD COLUMN IF NOT EXISTS) since this runs on a shared prod/dev database. The column addition must be idempotent.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit src/lib/contact-fiscal-store.ts 2>&1 | head -20</automated>
  </verify>
  <done>contact_fiscal table schema includes city TEXT column. Interface and upsert function handle city field. Column addition is idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Replace xmlContent city extraction with contact_fiscal lookup + add backfill</name>
  <files>src/app/api/customers/route.ts</files>
  <action>
1. Replace the city extraction block (lines 150-173) with a query against contact_fiscal:

```sql
SELECT cnpj, city FROM contact_fiscal
WHERE company_id = $1 AND cnpj = ANY($2) AND city IS NOT NULL
```

Use `prisma.$queryRawUnsafe` with company.id and allCnpjs. Build cityByKey map from results. Wrap in try/catch for table-not-exists fallback (return empty map).

2. Delete the entire `latestXmls` query and the regex parsing loop. The customers route should have ZERO xmlContent references after this plan (Plan 04-01 already removed the product counting xmlContent usage).

3. Create a one-time backfill endpoint or inline script. The simplest approach: add a backfill function at the bottom of contact-fiscal-store.ts (or as a separate file `src/lib/backfill-contact-city.ts`):

```typescript
export async function backfillContactFiscalCity(companyId: string): Promise<number> {
  // Find contact_fiscal rows with city IS NULL
  // For each, find the most recent Invoice with xmlContent
  // Extract xMun + UF from enderDest (for issued) or enderEmit (for received)
  // UPDATE contact_fiscal SET city = extracted_city WHERE id = row.id
  // Process in batches of 100
}
```

The backfill should process in batches to avoid loading too much XML. Use DISTINCT ON (cnpj) to get one invoice per CNPJ, extract city via regex, and update contact_fiscal.

4. Add a call to the backfill at the end of the customers route GET handler, but only if there are customers with null cities (lazy backfill). Use a lightweight check: if any customer in the result has city=null AND we have CNPJs, trigger backfill in background (do not await — fire and forget). The next request will have the data.

Actually, simpler approach: just run the backfill as part of the contact_fiscal ensureTable init (once per server start). Add it as a post-init step that checks if any rows have city IS NULL and backfills them.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && grep -c "xmlContent" src/app/api/customers/route.ts</automated>
  </verify>
  <done>Customers route has zero xmlContent references. City data comes from contact_fiscal table. Backfill function exists to populate city for existing contact_fiscal rows from XML data on first run.</done>
</task>

</tasks>

<verification>
1. `grep -n "xmlContent" src/app/api/customers/route.ts` returns zero matches
2. `grep -n "contact_fiscal" src/app/api/customers/route.ts` shows city query
3. `grep -n "city" src/lib/contact-fiscal-store.ts` shows new column and interface field
4. TypeScript compiles without errors for both files
</verification>

<success_criteria>
- contact_fiscal table has city TEXT column (added idempotently)
- Customers route reads city from contact_fiscal, never from xmlContent
- Backfill mechanism exists to populate city for existing records
- Zero xmlContent references remain in customers/route.ts
</success_criteria>

<output>
After completion, create `.planning/phases/04-xml-extraction-performance/04-04-SUMMARY.md`
</output>
