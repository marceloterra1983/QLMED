---
phase: 08-file-splitting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(painel)/sistema/settings/page-client.tsx
  - src/app/(painel)/sistema/settings/components/CompanySettingsTab.tsx
  - src/app/(painel)/sistema/settings/components/UserManagementTab.tsx
  - src/app/(painel)/sistema/settings/components/SystemInfoTab.tsx
  - src/app/(painel)/financeiro/contas-pagar/page-client.tsx
  - src/app/(painel)/financeiro/contas-receber/page-client.tsx
  - src/app/(painel)/financeiro/components/DuplicataEditPanel.tsx
  - src/app/(painel)/financeiro/components/FinanceiroTable.tsx
  - src/app/(painel)/financeiro/components/financeiro-utils.ts
  - src/lib/local-xml-sync.ts
  - src/lib/local-xml-sync/onedrive-client.ts
  - src/lib/local-xml-sync/file-import.ts
  - src/lib/local-xml-sync/sync-scheduler.ts
  - src/lib/local-xml-sync/sync-types.ts
  - src/lib/local-xml-sync/sync-utils.ts
autonomous: true
requirements:
  - SPLIT-04

must_haves:
  truths:
    - "Settings page works identically with all tabs after split"
    - "Financeiro contas-pagar and contas-receber pages work identically"
    - "Local XML sync functionality works identically"
    - "No file in src/ exceeds 800 lines (except generated schemas)"
  artifacts:
    - path: "src/app/(painel)/sistema/settings/components/"
      provides: "Settings tab components split by concern"
    - path: "src/app/(painel)/financeiro/components/"
      provides: "Shared financeiro sub-components"
    - path: "src/lib/local-xml-sync/"
      provides: "XML sync modules split by responsibility"
  key_links:
    - from: "settings/page-client.tsx"
      to: "settings/components/*.tsx"
      via: "import tab components"
      pattern: "import.*from.*components/"
    - from: "contas-pagar/page-client.tsx"
      to: "financeiro/components/*.tsx"
      via: "import shared components"
      pattern: "import.*from.*components/"
---

<objective>
Split remaining large files: settings/page-client (1298), financeiro pages (1159 each), and local-xml-sync (1115) into cohesive modules under 500 lines each.

Purpose: These are the remaining files exceeding the 800-line target. Splitting them completes the phase 8 goal of zero oversized files.
Output: All four file groups split into modules, no src/ file exceeds 800 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(painel)/sistema/settings/page-client.tsx
@src/app/(painel)/financeiro/contas-pagar/page-client.tsx
@src/lib/local-xml-sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split settings page-client and financeiro pages</name>
  <files>
    src/app/(painel)/sistema/settings/page-client.tsx
    src/app/(painel)/sistema/settings/components/CompanySettingsTab.tsx
    src/app/(painel)/sistema/settings/components/UserManagementTab.tsx
    src/app/(painel)/sistema/settings/components/SystemInfoTab.tsx
    src/app/(painel)/financeiro/contas-pagar/page-client.tsx
    src/app/(painel)/financeiro/contas-receber/page-client.tsx
    src/app/(painel)/financeiro/components/DuplicataEditPanel.tsx
    src/app/(painel)/financeiro/components/FinanceiroTable.tsx
    src/app/(painel)/financeiro/components/financeiro-utils.ts
  </files>
  <action>
**Settings page (1298 lines):**

Create `src/app/(painel)/sistema/settings/components/` directory.

1. Read settings/page-client.tsx to identify tab boundaries. The page has a tab-based layout. Extract each major tab into its own component:
   - **CompanySettingsTab.tsx** — Company info editing (name, CNPJ, address, logo, etc.)
   - **UserManagementTab.tsx** — User list, role management, invite/remove
   - **SystemInfoTab.tsx** — System info display (version, storage stats, health, `formatBytes` helper)
   - Keep page-client.tsx as tab router: manages active tab state, renders the selected tab component
   - Target: page-client.tsx under 200 lines, each tab under 500 lines

**Financeiro pages (1159 lines each):**

Create `src/app/(painel)/financeiro/components/` directory.

Both contas-pagar and contas-receber page-clients share the same structure (Phase 5 made them parametrized but each file is still large). Extract shared visual components:

1. **financeiro-utils.ts** — Extract shared utility functions that are duplicated in both pages:
   - `parseCurrencyInput()`, `roundMoney()`, `toCurrencyInput()`, `getNextDupNumero()`, `createEditRowId()`
   - `statusConfig` constant (line ~128)

2. **DuplicataEditPanel.tsx** — Extract the duplicata editing panel (the large inline edit form for installments/duplicatas). This is the bulkiest JSX section in both pages, with inputs for dupNumero, dVenc, vDup, add/remove rows. Props: editRows, onChange, onAdd, onRemove, direction.

3. **FinanceiroTable.tsx** — Extract the main table rendering (invoice rows with expand, status badges, action buttons). Props: invoices, expandedId, handlers.

4. Rewrite both page-client.tsx files to import from `../components/`. Each should be under 500 lines.

NOTE: contas-pagar and contas-receber may already share code via `@/lib/financeiro-shared.ts` (Phase 5 DUP-02). Check imports before extracting — do not duplicate what already exists. The target here is UI component extraction, not business logic.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30 && wc -l src/app/\(painel\)/sistema/settings/page-client.tsx src/app/\(painel\)/financeiro/contas-pagar/page-client.tsx src/app/\(painel\)/financeiro/contas-receber/page-client.tsx</automated>
  </verify>
  <done>Settings page-client under 200 lines with 3 tab components. Both financeiro page-clients under 500 lines with shared components. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Split local-xml-sync.ts into modules</name>
  <files>
    src/lib/local-xml-sync.ts
    src/lib/local-xml-sync/onedrive-client.ts
    src/lib/local-xml-sync/file-import.ts
    src/lib/local-xml-sync/sync-scheduler.ts
    src/lib/local-xml-sync/sync-types.ts
    src/lib/local-xml-sync/sync-utils.ts
  </files>
  <action>
The file at 1115 lines handles: OneDrive API calls, local file scanning, XML import/parsing, queuing, and scheduling. Split by responsibility.

Create `src/lib/local-xml-sync/` directory. The original `src/lib/local-xml-sync.ts` file will become the re-exporting entry point (or be replaced by the directory with an `index.ts`).

1. **sync-types.ts** — Extract all types/interfaces:
   - `OneDriveConnection`, `TargetCompany`, and any other shared types
   - Import Prisma types as needed

2. **sync-utils.ts** — Extract pure utility functions (lines 91-180):
   - `resolveConfiguredDir`, `isXmlFile`, `isPdfFile`, `shouldIgnoreByPath`
   - `getErrorCode`, `chunkArray`, `getDelayUntilNextHalfHourMs`
   - `extractAccessKeyFromFilePath`, `normalizeOneDrivePath`

3. **onedrive-client.ts** — Extract all OneDrive API functions (lines 181-310):
   - `ensureValidOneDriveAccessTokenLocal`
   - `oneDriveGraphJsonRequest`, `oneDriveGraphDownloadFile`
   - `listOneDriveChildrenAll`, `resolveOneDriveItemByPath`
   - `copyOneDriveFileIfNeeded`, `copyOneDriveXmlFileIfNeeded`, `copyOneDrivePdfFileIfNeeded`

4. **file-import.ts** — Extract file import and DB interaction (lines 293-670):
   - `directoryExists`, `getTargetCompany`, `getNewestMonthFolder`, `getMostRecentMonthFolders`
   - `collectAllXmlFiles`, `findFilesMissingInDatabase`
   - `rememberFileFingerprint`, `copyXmlFileIfNeeded`
   - `importXmlFile`, `enqueueImport`, `drainImportQueue`

5. **sync-scheduler.ts** — Extract reconciliation and scheduling (line 674+):
   - `reconcileLatestFolder`, `reconcileFolder`
   - Any interval/timer management functions
   - The main sync loop/scheduler

6. **Update local-xml-sync.ts** (or create index.ts):
   - Re-export public API from sub-modules
   - Ensure all existing imports of `@/lib/local-xml-sync` continue to work
   - If the original file is imported as `@/lib/local-xml-sync`, the directory with `index.ts` approach works seamlessly with Node/Next.js resolution

Check all files in the codebase that import from `@/lib/local-xml-sync` and ensure they still resolve correctly after the split.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30 && wc -l src/lib/local-xml-sync/*.ts && echo "---" && find src/ -name "*.ts" -o -name "*.tsx" | xargs wc -l | sort -rn | head -20</automated>
  </verify>
  <done>local-xml-sync.ts split into 5 modules under local-xml-sync/ directory. Each module under 500 lines. All imports resolve. The final `find` command confirms no src/ file exceeds 800 lines (except generated schemas).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No file in src/ exceeds 800 lines: `find src/ -name "*.ts" -o -name "*.tsx" | xargs wc -l | sort -rn | head -20`
- `npm run build` completes without errors
- Settings page renders all tabs correctly
- Financeiro pages load and edit duplicatas correctly
- Local XML sync (if testable) resolves imports
</verification>

<success_criteria>
- settings/page-client reduced from 1298 to under 200 lines
- Both financeiro page-clients reduced from 1159 to under 500 lines each
- local-xml-sync.ts split into 5 modules, each under 500 lines
- No src/ file exceeds 800 lines (except generated schemas like prisma client)
- Zero TypeScript errors
- All page functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-splitting/08-04-SUMMARY.md`
</output>
