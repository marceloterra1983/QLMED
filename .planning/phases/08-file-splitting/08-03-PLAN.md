---
phase: 08-file-splitting
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SupplierDetailsModal.tsx
  - src/components/CustomerDetailsModal.tsx
  - src/components/contact-details/ContactInfoSection.tsx
  - src/components/contact-details/AddressSection.tsx
  - src/components/contact-details/FiscalSection.tsx
  - src/components/contact-details/PriceTableSection.tsx
  - src/components/contact-details/contact-detail-utils.ts
  - src/components/contact-details/contact-detail-types.ts
autonomous: true
requirements:
  - SPLIT-03

must_haves:
  truths:
    - "Supplier and Customer detail modals render identically after refactor"
    - "Shared tab sub-components are reused by both modals"
    - "Each modal file is under 500 lines"
  artifacts:
    - path: "src/components/contact-details/contact-detail-types.ts"
      provides: "Shared types/interfaces for contact detail sections"
    - path: "src/components/contact-details/contact-detail-utils.ts"
      provides: "Shared helpers: InfoField, EditableField, SectionCard, StatCard, normalizeForCompare, compareAddressFields"
    - path: "src/components/contact-details/ContactInfoSection.tsx"
      provides: "Contact info display (name, CNPJ, IE, phone, email) with edit capability"
    - path: "src/components/contact-details/AddressSection.tsx"
      provides: "Address display and edit section"
    - path: "src/components/contact-details/FiscalSection.tsx"
      provides: "Fiscal data section (CNAE, regime, IE status)"
    - path: "src/components/contact-details/PriceTableSection.tsx"
      provides: "Price table with search, sort, pagination"
  key_links:
    - from: "SupplierDetailsModal.tsx"
      to: "contact-details/*.tsx"
      via: "import shared sections"
      pattern: "import.*from.*contact-details/"
    - from: "CustomerDetailsModal.tsx"
      to: "contact-details/*.tsx"
      via: "import shared sections"
      pattern: "import.*from.*contact-details/"
---

<objective>
Extract shared tab sub-components from SupplierDetailsModal (1520 lines) and CustomerDetailsModal (1469 lines) into reusable components under `src/components/contact-details/`.

Purpose: These two modals are ~80% identical (same sections: info, address, fiscal, price table, invoices). Extracting shared sections eliminates duplication and makes each modal maintainable.
Output: Both modals under 500 lines each, 4+ shared sub-components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/SupplierDetailsModal.tsx
@src/components/CustomerDetailsModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared contact-details components</name>
  <files>
    src/components/contact-details/contact-detail-types.ts
    src/components/contact-details/contact-detail-utils.ts
    src/components/contact-details/ContactInfoSection.tsx
    src/components/contact-details/AddressSection.tsx
    src/components/contact-details/FiscalSection.tsx
    src/components/contact-details/PriceTableSection.tsx
  </files>
  <action>
Create `src/components/contact-details/` directory.

1. **contact-detail-types.ts** — Define shared types:
   - Props interfaces for each section component
   - Generic contact type that works for both supplier and customer
   - PriceRow generic type (both modals have priceTable with similar shape)
   - EditDraft type for editable fields
   - SortKey type for price table

2. **contact-detail-utils.ts** — Extract duplicated helpers that exist in BOTH modals:
   - `normalizeForCompare()` (Supplier line 170, Customer line 127)
   - `compareAddressFields()` (Supplier line 175, Customer line 132)
   - `InfoField` component (Supplier line 209, Customer line 168)
   - `EditableField` component (Supplier line 218, Customer line 177)
   - `SectionCard` component (Supplier line 262, Customer line 221)
   - `StatCard` component (Supplier line 308, Customer line 266)
   - `thCls` and `tdCls` constants (both files)
   All are nearly identical between the two modals — use the more complete version.

3. **AddressSection.tsx** — Extract the address display/edit block:
   - Shows street, number, complement, district, city, state, zipCode
   - Supports edit mode via EditableField
   - Props: address data, edit mode flag, draft state, onChange handler

4. **PriceTableSection.tsx** — Extract the price table with search/sort:
   - Search input for filtering
   - Sortable column headers (SortableHeader sub-component)
   - Table rows with product details, quantities, prices
   - Props: priceTable data array, entity type ('supplier' | 'customer')
   - This is the largest shared section (~300 lines in each modal)

5. **ContactInfoSection.tsx** — Extract the basic contact info display:
   - Name, CNPJ/CPF, IE, phone, email fields
   - Edit mode support
   - Props: contact data, edit state

6. **FiscalSection.tsx** — Extract fiscal data display:
   - CNAE, tax regime, IE status, fiscal checks
   - Props: fiscal data, CNAE mismatch info

All .tsx files need `'use client'` directive. Import shared utils from contact-detail-utils.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>6 shared files created under src/components/contact-details/. Each exports reusable components with typed props. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SupplierDetailsModal and CustomerDetailsModal to use shared components</name>
  <files>
    src/components/SupplierDetailsModal.tsx
    src/components/CustomerDetailsModal.tsx
  </files>
  <action>
1. **SupplierDetailsModal.tsx** — Rewrite to import and use shared components:
   - Remove all inline helper functions/components now in contact-detail-utils
   - Remove duplicated section JSX now in shared components
   - Import: `ContactInfoSection`, `AddressSection`, `FiscalSection`, `PriceTableSection` from contact-details/
   - Import: `SectionCard`, `StatCard`, `InfoField`, `EditableField` from contact-detail-utils
   - Keep: supplier-specific logic (CNAE mismatch checking via `checkCnaeMismatchClient` at line 121), modal open/close, data fetching, edit/save
   - Keep: the modal shell (overlay, header, tab navigation, footer)
   - Wire shared components with supplier-specific props
   - Target: under 500 lines

2. **CustomerDetailsModal.tsx** — Same approach:
   - Remove all duplicated helpers and sections
   - Import shared components
   - Keep: customer-specific logic, modal shell, data fetching, edit/save
   - Wire shared components with customer-specific props
   - Target: under 500 lines

Both modals must render identically to before. The visual appearance, edit functionality, price table sorting/filtering, and invoice tab must all work exactly the same.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30 && wc -l src/components/SupplierDetailsModal.tsx src/components/CustomerDetailsModal.tsx src/components/contact-details/*.tsx src/components/contact-details/*.ts</automated>
  </verify>
  <done>SupplierDetailsModal.tsx under 500 lines. CustomerDetailsModal.tsx under 500 lines. Both import from shared contact-details/ components. Zero TypeScript errors. Visual and functional behavior unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `wc -l` on both modal files shows < 500 lines each
- `wc -l` on shared components shows < 500 lines each
- `npm run build` completes without errors
</verification>

<success_criteria>
- SupplierDetailsModal reduced from 1520 to under 500 lines
- CustomerDetailsModal reduced from 1469 to under 500 lines
- 4+ shared components created under contact-details/
- Zero TypeScript errors
- Modal functionality (view, edit, save, price table, invoices) preserved for both
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-splitting/08-03-SUMMARY.md`
</output>
