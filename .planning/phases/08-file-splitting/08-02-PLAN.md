---
phase: 08-file-splitting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/invoices/[id]/pdf/route.ts
  - src/lib/pdf/pdf-utils.ts
  - src/lib/pdf/pdf-types.ts
  - src/lib/pdf/danfe-generator.ts
  - src/lib/pdf/dacte-generator.ts
  - src/lib/pdf/nfse-generator.ts
  - src/lib/pdf/pdf-css.ts
autonomous: true
requirements:
  - SPLIT-02

must_haves:
  truths:
    - "PDF generation for NF-e, CT-e, and NFS-e works identically after split"
    - "Each generator module has less than 500 lines"
    - "pdf/route.ts is reduced to a thin router under 200 lines"
  artifacts:
    - path: "src/lib/pdf/pdf-types.ts"
      provides: "All interfaces: DanfeData, DanfeProduct, CteData, CtePartyData, NfseData, PdfInvoiceView, Party"
    - path: "src/lib/pdf/pdf-utils.ts"
      provides: "Shared helpers: parseXml, esc, fmtCnpj, fmtCep, fmtFone, fmtNum, fmtCurrency, fmtKey, fmtDate, fmtTime, etc."
    - path: "src/lib/pdf/pdf-css.ts"
      provides: "Shared CSS constant for all PDF generators"
    - path: "src/lib/pdf/danfe-generator.ts"
      provides: "extractDanfeData + buildDanfeHtml"
      exports: ["extractDanfeData", "buildDanfeHtml"]
    - path: "src/lib/pdf/dacte-generator.ts"
      provides: "extractCteData, buildCteDataFromInvoice, buildCteHtml"
      exports: ["extractCteData", "buildCteDataFromInvoice", "buildCteHtml"]
    - path: "src/lib/pdf/nfse-generator.ts"
      provides: "extractNfseData, buildNfseHtml"
      exports: ["extractNfseData", "buildNfseHtml"]
  key_links:
    - from: "src/app/api/invoices/[id]/pdf/route.ts"
      to: "src/lib/pdf/*"
      via: "import generators and dispatch by invoice type"
      pattern: "import.*from.*@/lib/pdf/"
---

<objective>
Split invoices/[id]/pdf/route.ts (2279 lines) into separate PDF generator modules under `src/lib/pdf/`.

Purpose: The PDF route contains three completely independent document generators (DANFE for NF-e, DACTE for CT-e, NFS-e) mixed together. Splitting by document type makes each generator independently maintainable and testable.
Output: Route file reduced to thin router (~100 lines), 4+ modules under src/lib/pdf/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/api/invoices/[id]/pdf/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pdf-types, pdf-utils, pdf-css, and extract DANFE generator</name>
  <files>
    src/lib/pdf/pdf-types.ts
    src/lib/pdf/pdf-utils.ts
    src/lib/pdf/pdf-css.ts
    src/lib/pdf/danfe-generator.ts
  </files>
  <action>
Create `src/lib/pdf/` directory.

1. **pdf-types.ts** — Extract ALL interfaces and types from route.ts:
   - `PdfInvoiceView` (line 102)
   - `DanfeProduct`, `DanfeParcela`, `DanfeData` (lines 130-228)
   - `CtePartyData`, `CteDocRef`, `CteComp`, `CteCargaMedida`, `CteObsCont`, `CteData` (lines 230-307)
   - `Party` (line 309)
   - `NfsePartyData`, `NfseData` (lines 1668-1710)
   Export all as named exports.

2. **pdf-utils.ts** — Extract shared helper functions (lines 14-127):
   - `parseXml`, `esc`, `fmtCnpj`, `fmtCep`, `fmtFone`, `fmtNum`, `fmtCurrency`, `fmtKey`, `fmtNfNum`, `fmtDate`, `fmtTime`, `fmtDateTime`
   - `modFreteLabel`, `modFreteCode`
   - `getPdfFilename`
   - `hasParty`, `getParty` (lines 314-324)
   Import types from pdf-types. Export all functions.

3. **pdf-css.ts** — Extract the CSS constant (lines 887-944):
   - Export `const PDF_CSS: string`

4. **danfe-generator.ts** — Extract DANFE (NF-e) generation:
   - `extractDanfeData` (line 717)
   - `extractIcmsFromImposto`, `extractIpiFromImposto` (lines 862-883)
   - All `build*` HTML section functions (lines 948-1363): `buildCanhoto`, `buildHeader`, `buildDest`, `buildFatura`, `buildImpostos`, `buildTransporte`, `buildProducts`, `buildAdditional`
   - `buildDanfeHtml` (line 1367)
   - Also include `buildFallbackHtml` (line 2073) — it's a simple fallback that doesn't belong to CTE or NFSE
   - Import from pdf-types, pdf-utils, pdf-css
   - Export: `extractDanfeData`, `buildDanfeHtml`, `buildFallbackHtml`
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Four new files created under src/lib/pdf/. All types extracted, shared utils extracted, DANFE generator is self-contained. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extract DACTE, NFS-e generators and rewrite route.ts as thin router</name>
  <files>
    src/lib/pdf/dacte-generator.ts
    src/lib/pdf/nfse-generator.ts
    src/app/api/invoices/[id]/pdf/route.ts
  </files>
  <action>
1. **dacte-generator.ts** — Extract CT-e (DACTE) generation:
   - All CT-e helper functions (lines 325-500): `getCteModalLabel`, `getCteTpServLabel`, `parseCteTomador`, `getCteTypeLabel`, `getGlobalizadoLabel`, `normalizeCteParty`, `parseNfeKey`, `formatDocRefNumber`, `emptyCtePartyData`, `fmtCteNumber`, `fmtCteSerie`, `fmtCteModelo`, `cteUnidadeLabel`, `formatCteQtdMedida`, `formatCtePartyAddress`, `formatCtePartyCity`
   - `extractCteData` (line 501)
   - `buildCteDataFromInvoice` (line 664)
   - `buildCteHtml` (line 1401)
   - Import from pdf-types, pdf-utils, pdf-css
   - Export: `extractCteData`, `buildCteDataFromInvoice`, `buildCteHtml`

2. **nfse-generator.ts** — Extract NFS-e generation (lines 1666-2069):
   - `emptyNfseParty`, `normalizeNfseParty`, `ibgeMunName`
   - `extractNfseData` (line 1746)
   - `buildNfseHtml` (line 1898)
   - Import from pdf-types, pdf-utils, pdf-css
   - Export: `extractNfseData`, `buildNfseHtml`

3. **Rewrite route.ts** — Replace all extracted code with imports. The route becomes a thin dispatcher:
   - Import `extractDanfeData`, `buildDanfeHtml`, `buildFallbackHtml` from `@/lib/pdf/danfe-generator`
   - Import `extractCteData`, `buildCteDataFromInvoice`, `buildCteHtml` from `@/lib/pdf/dacte-generator`
   - Import `extractNfseData`, `buildNfseHtml` from `@/lib/pdf/nfse-generator`
   - Import `parseXml`, `getPdfFilename` from `@/lib/pdf/pdf-utils`
   - Import types from `@/lib/pdf/pdf-types`
   - Keep ONLY the `GET` handler function that: fetches invoice from DB, dispatches to correct generator based on type, renders with Puppeteer, returns PDF
   - Target: under 150 lines

Do NOT change any behavior. The PDF output must be byte-identical for all document types.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30 && wc -l src/app/api/invoices/\[id\]/pdf/route.ts src/lib/pdf/*.ts</automated>
  </verify>
  <done>route.ts is under 150 lines. All 6 files under src/lib/pdf/ exist. Each file is under 500 lines. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `wc -l` on route.ts shows < 200 lines
- `wc -l` on each pdf module shows < 500 lines
- `npm run build` completes without errors
</verification>

<success_criteria>
- PDF route reduced from 2279 to under 200 lines
- 6 module files created under src/lib/pdf/, each under 500 lines
- Zero TypeScript errors
- PDF generation for NF-e, CT-e, NFS-e unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-splitting/08-02-SUMMARY.md`
</output>
