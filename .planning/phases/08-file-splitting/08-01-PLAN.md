---
phase: 08-file-splitting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(painel)/cadastro/produtos/page-client.tsx
  - src/app/(painel)/cadastro/produtos/components/ProductDetailModal.tsx
  - src/app/(painel)/cadastro/produtos/components/BulkEditModal.tsx
  - src/app/(painel)/cadastro/produtos/components/ProductFilters.tsx
  - src/app/(painel)/cadastro/produtos/components/ProductTable.tsx
  - src/app/(painel)/cadastro/produtos/components/ExportCSVButton.tsx
  - src/app/(painel)/cadastro/produtos/components/product-utils.ts
autonomous: true
requirements:
  - SPLIT-01

must_haves:
  truths:
    - "Pagina de produtos carrega e funciona identicamente apos split"
    - "Cada componente extraido tem menos de 500 linhas"
    - "page-client.tsx principal tem menos de 500 linhas"
  artifacts:
    - path: "src/app/(painel)/cadastro/produtos/components/ProductDetailModal.tsx"
      provides: "Product detail inline modal with sections, edit fields, purchase/sales history"
    - path: "src/app/(painel)/cadastro/produtos/components/BulkEditModal.tsx"
      provides: "Bulk edit overlay for multi-selected products"
    - path: "src/app/(painel)/cadastro/produtos/components/ProductFilters.tsx"
      provides: "Filter bar, search, group-by, sort controls"
    - path: "src/app/(painel)/cadastro/produtos/components/ProductTable.tsx"
      provides: "Product table rows with selection, grouping, inline expand"
    - path: "src/app/(painel)/cadastro/produtos/components/ExportCSVButton.tsx"
      provides: "CSV export button with column selection"
    - path: "src/app/(painel)/cadastro/produtos/components/product-utils.ts"
      provides: "Shared utility functions and constants (normalizeSearch, formatters, CSS constants)"
    - path: "src/app/(painel)/cadastro/produtos/page-client.tsx"
      provides: "Orchestrator: state management, data fetching, wiring sub-components"
  key_links:
    - from: "page-client.tsx"
      to: "components/*.tsx"
      via: "import and props passing"
      pattern: "import.*from.*components/"
---

<objective>
Split produtos/page-client.tsx (3622 lines) into 5+ cohesive components under a `components/` subdirectory.

Purpose: The largest file in the codebase is unmaintainable. Splitting by domain responsibility makes each piece readable and independently modifiable.
Output: page-client.tsx reduced to ~400 lines orchestrator, 5+ sub-components each under 500 lines.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(painel)/cadastro/produtos/page-client.tsx
@src/app/(painel)/cadastro/produtos/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract utility functions and shared sub-components into modules</name>
  <files>
    src/app/(painel)/cadastro/produtos/components/product-utils.ts
    src/app/(painel)/cadastro/produtos/components/ProductDetailModal.tsx
    src/app/(painel)/cadastro/produtos/components/BulkEditModal.tsx
  </files>
  <action>
Create `components/` directory under `src/app/(painel)/cadastro/produtos/`.

1. **product-utils.ts** — Extract all standalone utility functions and constants from page-client.tsx:
   - `normalizeSearch()` (line 15)
   - `formatQuantity()` (line 23)
   - `formatDate()` (line 27)
   - `getAnvisaExpirationBadge()` (line 33)
   - `formatOptional()` (line 52)
   - `highlightMatch()` (line 57)
   - `iconBgMap` constant (line 76)
   - `DETAIL_INPUT_CLS` constant (line 87)
   - `BULK_INPUT_CLS` constant (line 122)
   - `bulkFieldIconMap` constant (line 124)
   - `DetailSectionCard` component (line 89)
   - `DetailField` component (line 113)
   - `BulkFieldRow` component (line 133)
   Export all from this file.

2. **ProductDetailModal.tsx** — Extract the inline product detail modal. In page-client.tsx, the detail modal starts around the product detail state block (line ~351) and renders a slide-over panel with collapsible sections (identification, fiscal, ANVISA, pricing, history). The JSX for this starts around line ~2540 (the detail panel render block). Extract:
   - All detail-related state hooks (detailProduct, detailData, openSections, editDraft, etc.)
   - The fetchProductDetail and update logic
   - Purchase/sales history fetching and display
   - The entire detail panel JSX
   - Props: `{ product: ProductRow | null, onClose: () => void, onUpdated: () => void }`
   - Import `DetailSectionCard`, `DetailField`, `DETAIL_INPUT_CLS` from product-utils

3. **BulkEditModal.tsx** — Extract the bulk edit overlay. In page-client.tsx, bulk edit state starts at line ~264 and the JSX is a full-screen overlay. Extract:
   - Bulk edit state (bulkEditOpen, bulkDraft, bulkChecked, etc.)
   - The handleBulkSave function
   - The overlay JSX
   - Props: `{ selectedIds: Set<string>, products: ProductRow[], onClose: () => void, onSaved: () => void }`
   - Import `BulkFieldRow`, `BULK_INPUT_CLS`, `bulkFieldIconMap` from product-utils

Keep `'use client'` directive on both .tsx files. Ensure all imports (React, toast, hooks, types) are correctly moved.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Three new files created. product-utils.ts exports all shared functions/constants. ProductDetailModal.tsx and BulkEditModal.tsx are self-contained components with typed props.</done>
</task>

<task type="auto">
  <name>Task 2: Extract ProductFilters, ProductTable, ExportCSV and wire page-client.tsx</name>
  <files>
    src/app/(painel)/cadastro/produtos/components/ProductFilters.tsx
    src/app/(painel)/cadastro/produtos/components/ProductTable.tsx
    src/app/(painel)/cadastro/produtos/components/ExportCSVButton.tsx
    src/app/(painel)/cadastro/produtos/page-client.tsx
  </files>
  <action>
1. **ProductFilters.tsx** — Extract the filter/search/sort controls bar. This includes:
   - Search input with debounce
   - Group-by dropdown
   - Sort field/direction controls
   - Line/group/subgroup filter dropdowns
   - ANVISA filter toggle
   - Active filter count badge
   - Props: receive current filter state and setter callbacks from parent

2. **ProductTable.tsx** — Extract the main product table rendering. This includes:
   - Table header with sortable columns
   - Group headers with collapse toggles
   - Product rows with checkbox selection, inline data display
   - Row click to open detail
   - Select-all checkbox logic (visible keys)
   - Props: receive products array, selected set, sort state, grouping, handlers from parent

3. **ExportCSVButton.tsx** — Extract the CSV export functionality:
   - Column selection popover
   - CSV generation and download
   - Props: products array, selected IDs (export selected or all)

4. **Rewrite page-client.tsx** — After extracting all 5 sub-components, page-client.tsx becomes an orchestrator:
   - Keeps all `useState` declarations for top-level state
   - Keeps `useEffect` for data fetching (loadProducts, loadSettings)
   - Keeps the `useMemo` computations for filtered/grouped data
   - Imports and renders: `ProductFilters`, `ProductTable`, `ProductDetailModal`, `BulkEditModal`, `ExportCSVButton`
   - Passes state down as props, receives callbacks up
   - Target: under 500 lines

Verify every interactive feature still works: search, filter, sort, group, select, bulk edit, detail modal, CSV export, auto-classify, pagination. Do NOT change any behavior, only move code.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npx tsc --noEmit --pretty 2>&1 | head -30 && wc -l src/app/\(painel\)/cadastro/produtos/page-client.tsx src/app/\(painel\)/cadastro/produtos/components/*.tsx src/app/\(painel\)/cadastro/produtos/components/*.ts</automated>
  </verify>
  <done>page-client.tsx is under 500 lines. All 5+ extracted components exist under components/. Each component is under 500 lines. TypeScript compiles without errors. No behavioral changes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `wc -l` on page-client.tsx shows < 500 lines
- `wc -l` on each component file shows < 500 lines
- `npm run build` completes without errors (test the produtos page specifically)
</verification>

<success_criteria>
- page-client.tsx reduced from 3622 to under 500 lines
- 5+ component files created, each under 500 lines
- Zero TypeScript errors
- All product page functionality preserved (search, filter, sort, group, select, bulk edit, detail, export)
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-splitting/08-01-SUMMARY.md`
</output>
