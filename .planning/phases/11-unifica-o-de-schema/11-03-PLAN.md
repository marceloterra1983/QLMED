---
phase: 11-unifica-o-de-schema
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/marce/CLAUDE.md
  - .github/workflows/deploy-production.yml
autonomous: true
requirements: [SCHEMA-03]

> **CORREÇÃO PÓS PLAN-CHECK (2026-07-11)**: `CLAUDE.md` referenciado neste
> plano é `/home/marce/CLAUDE.md` — um arquivo em REPOSITÓRIO GIT DIFERENTE
> do repo `app-dev` onde este plano roda (confirmado: `git -C
> /home/marce/qlmed/app-dev ls-files` não lista nenhum `CLAUDE.md`; o app-dev
> não tem esse arquivo). A Task 1 abaixo produz um commit SEPARADO no repo
> `/home/marce`, fora do fluxo normal de commit desta fase (que roda dentro
> de `app-dev`) — o executor deve fazer `cd /home/marce` antes de editar e
> commitar esse arquivo especificamente, com `git commit -- CLAUDE.md`
> (pathspec explícito, nunca `git add -A`, já que outros processos podem
> estar escrevendo no mesmo repo /home/marce em paralelo). A Task 2
> (deploy-production.yml) continua normal, dentro do repo app-dev.

must_haves:
  truths:
    - "Existe uma seção documentada em CLAUDE.md explicando a política expand/contract para migrações futuras do schema Prisma"
    - "O workflow de deploy documenta explicitamente que rollback de imagem não desfaz migração de banco"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Seção 'Database — Migration Policy (expand/contract)' com regras para migrações futuras"
      contains: "expand/contract"
    - path: ".github/workflows/deploy-production.yml"
      provides: "Comentário explícito no step de rollback esclarecendo que apenas a imagem do container é revertida, não o schema do banco"
      contains: "não desfaz migração"
  key_links:
    - from: "CLAUDE.md"
      to: ".github/workflows/deploy-production.yml"
      via: "referência textual cruzada (CLAUDE.md cita o step de rollback; o workflow cita a política em CLAUDE.md)"
      pattern: "expand/contract|migration"
---

<objective>
Documentar formalmente a política de migração "expand/contract" (mudanças
sempre aditivas e retrocompatíveis com a imagem de container anterior antes
de qualquer remoção/renomeação) em CLAUDE.md, e deixar explícito no workflow
de deploy que o rollback automático de imagem (`Roll back failed
deployment`) reverte apenas o container — nunca desfaz uma migração de
banco já aplicada por `migrate deploy`. Fecha SCHEMA-03.

Purpose: Sem essa política escrita, cada migração futura corre o risco de
quebrar um rollback de imagem (imagem antiga rodando contra schema novo).
Documentar agora, enquanto a Fase 11 já está mexendo em schema/migrações,
evita que a lição se perca depois que as 11 tabelas satélite forem todas
migradas.

Output: Nova seção em CLAUDE.md; comentário adicional no
`deploy-production.yml` (sem alterar nenhum comportamento do workflow —
apenas comentários/documentação inline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/home/marce/docs/server/ARCH-REMEDIATION-PLAN.md

<interfaces>
<!-- Estrutura atual relevante de .github/workflows/deploy-production.yml -->
- Step "Deploy stack on server" (linha ~91-103): roda
  `node node_modules/prisma/build/index.js migrate deploy && node
  node_modules/prisma/build/index.js migrate diff --from-config-datasource
  --to-schema prisma/schema.prisma --exit-code` dentro do container antes de
  subir `qlmed-app`. Isso já falha o deploy se houver drift pós-migração —
  não precisa de mudança funcional, só documentação.
- Step "Capture rollback image" (linha ~80-89): tagueia a imagem atual como
  `qlmed-app:rollback-${GITHUB_RUN_ID}` ANTES do rebuild/deploy.
- Step "Roll back failed deployment" (linha ~173-188): em caso de falha,
  sobe a imagem antiga via `docker compose up -d --no-build --force-recreate
  qlmed-app` usando a tag de rollback — isso NÃO executa `migrate` nem
  reverte nenhuma alteração de schema. É aqui que o comentário deve ser
  adicionado.

<!-- Seção do CLAUDE.md onde inserir a nova política -->
CLAUDE.md já tem uma seção "## Common Commands" com subsection
"### Deployment" e uma seção sobre banco de dados espalhada em "### DB proxy"
e no bloco de comandos "npm run db:*". A nova seção "Database — Migration
Policy (expand/contract)" deve ser inserida logo após a subsection
"### Deployment" (mesmo nível de heading ###), citando explicitamente o
step "Roll back failed deployment" do workflow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Documentar política expand/contract em CLAUDE.md (repo /home/marce, separado do app-dev)</name>
  <files>/home/marce/CLAUDE.md</files>
  <action>
    Este arquivo vive em `/home/marce/CLAUDE.md`, repo git diferente deste
    (`app-dev`) — usar `cd /home/marce` antes de editar/commitar. Adicionar
    uma nova subsection `### Database Migration Policy
    (expand/contract)` em `/home/marce/CLAUDE.md`, logo após `### Deployment`, cobrindo:
    (1) toda migração de schema deve ser aditiva primeiro — novas colunas
    sempre nullable ou com default, novas tabelas nunca substituem uma
    tabela em uso na mesma migração; (2) remover/renomear uma coluna ou
    tabela só é seguro depois que NENHUMA imagem de container em produção
    (incluindo a tag de rollback `qlmed-app:rollback-*` capturada no deploy
    anterior) ainda depende da forma antiga — na prática, esperar pelo
    menos um ciclo de deploy saudável (a limpeza da tag de rollback, feita
    pelo step "Clean run-scoped rollback tag") antes de uma migração
    "contract"; (3) `deploy-production.yml` já roda `prisma migrate deploy`
    seguido de `prisma migrate diff --exit-code` a cada deploy — isso
    detecta drift, mas NÃO gera rollback de schema; (4) o step "Roll back
    failed deployment" reverte apenas a imagem do container
    (`docker compose up --force-recreate` com a tag antiga) — se uma
    migração de banco já rodou com sucesso antes da falha (ex.: falha
    ocorreu em "Verify local health", não em "migrate deploy"), o rollback
    de imagem deixa a imagem ANTIGA rodando contra o schema NOVO; a política
    expand/contract torna isso seguro por construção, pois a imagem antiga
    deve conseguir operar contra o schema novo até a próxima migração
    "contract"; (5) para as 11 tabelas satélite da Fase 11, isso significa:
    NUNCA remover `ensureXxxTable()`/raw SQL de uma store no mesmo deploy em
    que a tabela ainda seria lida pela imagem anterior em caso de rollback —
    manter compatibilidade de leitura por pelo menos um ciclo de deploy
    saudável antes de simplificar o código da store.
  </action>
  <verify>
    <automated>grep -qi "expand/contract" CLAUDE.md && echo OK</automated>
  </verify>
  <done>Seção nova existe em CLAUDE.md, referenciando o step "Roll back failed deployment" e a tag qlmed-app:rollback-*.</done>
</task>

<task type="auto">
  <name>Task 2: Comentar explicitamente o step de rollback no workflow</name>
  <files>.github/workflows/deploy-production.yml</files>
  <action>
    No step `Roll back failed deployment` (linha ~173), adicionar um
    comentário (linha `#`) logo abaixo da linha `run: |` explicando: "Este
    rollback reverte apenas a imagem do container qlmed-app para a tag
    capturada antes do deploy (qlmed-app:rollback-\${GITHUB_RUN_ID}) — NÃO
    desfaz nenhuma migração de banco já aplicada pelo step 'Deploy stack on
    server' (prisma migrate deploy). Ver política de migração expand/contract
    em CLAUDE.md antes de qualquer migração que remova ou renomeie
    colunas/tabelas." Não alterar nenhuma linha executável do workflow —
    apenas inserir o comentário.
  </action>
  <verify>
    <automated>grep -qi "não desfaz" .github/workflows/deploy-production.yml && echo OK</automated>
  </verify>
  <done>Comentário presente no step de rollback, sem alteração de comportamento do workflow (diff só adiciona linhas de comentário).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plano é puramente documental (CLAUDE.md + comentários YAML); nenhuma superfície de execução ou dado sensível envolvida |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Repudiation | deploy-production.yml (comentário) | accept | Mudança é documentação inline, revisável via git diff/PR, sem efeito em runtime |
</threat_model>

<verification>
1. `grep -qi "expand/contract" CLAUDE.md` retorna sucesso.
2. `grep -qi "não desfaz" .github/workflows/deploy-production.yml` retorna sucesso.
3. `git diff .github/workflows/deploy-production.yml` mostra apenas linhas de comentário adicionadas (nenhuma linha executável alterada).
</verification>

<success_criteria>
- Política expand/contract documentada e referenciável (CLAUDE.md).
- Workflow de deploy deixa explícito que rollback de imagem não cobre rollback de schema (SCHEMA-03, critérios de sucesso #3 e #4 do ROADMAP).
</success_criteria>

<output>
Create `.planning/phases/11-unifica-o-de-schema/11-03-SUMMARY.md` when done
</output>
