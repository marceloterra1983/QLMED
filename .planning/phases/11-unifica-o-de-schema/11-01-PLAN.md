---
phase: 11-unifica-o-de-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*_baseline_satellite_tables_schema11/migration.sql
  - prisma/migrations/migration_lock.toml
autonomous: false
requirements: [SCHEMA-01]

must_haves:
  truths:
    - "As 11 tabelas satélite (invoice_tax_totals, invoice_item_tax, contact_fiscal, invoice_duplicata, ncm_cache, product_registry, stock_entry, nfe_entry_item, product_settings_catalog, cnpj_cache, cnpj_monitoring) têm modelo Prisma sem @@ignore"
    - "npx prisma migrate status contra produção não reporta migrations pendentes nem drift"
    - "Nenhuma linha de dado foi perdida ou alterada nas 11 tabelas (contagens antes/depois idênticas)"
    - "prisma generate produz Prisma Client com acessores tipados (prisma.invoiceTaxTotals, prisma.cnpjCache, etc.) sem quebrar o build"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "11 modelos satélite sem @@ignore, mapeados para as tabelas físicas já existentes"
      contains: "model CnpjCache"
    - path: "prisma/migrations/*_baseline_satellite_tables_schema11/migration.sql"
      provides: "Migração baseline registrando as 11 tabelas como parte do histórico Prisma"
  key_links:
    - from: "prisma/schema.prisma"
      to: "_prisma_migrations (produção)"
      via: "prisma migrate resolve --applied"
      pattern: "baseline_satellite_tables_schema11"
---

<objective>
Estabelecer uma migration baseline no Prisma que reconhece as 11 tabelas
satélite hoje `@@ignore` (9 stores, 11 modelos — inclui `NfeEntryItem` e
`CnpjMonitoring` além dos 9 originalmente listados) como parte do schema
versionado, sem recriar nenhuma tabela física e sem perder dados. Isso
dissolve metade do dual source-of-truth descrito em SCHEMA-01: o Prisma
Client passa a "enxergar" essas tabelas (acessores tipados disponíveis),
mas o código das stores continua usando `$executeRawUnsafe`/`ensureXxxTable()`
até a migração store-a-store (11-02 e followups).

Purpose: Remove o bloqueio histórico ("nunca prisma migrate contra banco
compartilhado dev/prod") agora que `qlmed_dev` existe isolado — provar o
padrão de baseline primeiro em dev, replicar em produção só com aprovação
humana explícita, dado que qualquer toque em `_prisma_migrations` de
produção é irreversível sem cuidado.

Output: `prisma/schema.prisma` com os 11 modelos sem `@@ignore`; uma nova
pasta de migração em `prisma/migrations/` registrada como aplicada tanto em
`qlmed_dev` quanto em produção; `prisma generate` rodado com sucesso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@/home/marce/docs/server/ARCH-REMEDIATION-PLAN.md
@/home/marce/docs/server/BACKUP-RESTORE.md

<interfaces>
<!-- Os 11 modelos JÁ TÊM campos, @map e índices corretos — só falta remover @@ignore. -->
<!-- Nenhuma mudança de campo é necessária. Apenas remover a diretiva @@ignore e o comentário
     "Model exists for schema visibility only" acima de cada um (substituir por comentário
     de baseline, ver Task 1). -->

Modelos e linhas atuais em prisma/schema.prisma (relativas ao arquivo completo):
- InvoiceTaxTotals (linha ~438, @@map("invoice_tax_totals"))
- InvoiceItemTax (linha ~461, @@map("invoice_item_tax"))
- ContactFiscal (linha ~496, @@map("contact_fiscal"))
- InvoiceDuplicata (linha ~515, @@map("invoice_duplicata"))
- NcmCache (linha ~535, @@map("ncm_cache"))
- ProductRegistry (linha ~549, @@map("product_registry"))
- StockEntry (linha ~622, @@map("stock_entry"))
- NfeEntryItem (linha ~665, @@map("nfe_entry_item")) — achado extra, gerido por src/lib/stock-entry-store.ts
- ProductSettingsCatalog (linha ~730, @@map("product_settings_catalog"))
- CnpjCache (linha ~750, @@map("cnpj_cache"))
- CnpjMonitoring (linha ~760, @@map("cnpj_monitoring")) — achado extra, gerido por src/lib/cnpj-monitor.ts

prisma.config.ts carrega `.env` via dotenv (não sobrescreve vars já setadas no
processo) e injeta `datasource.url = process.env.DATABASE_URL`. Isso permite
sobrepor o alvo do CLI passando `DATABASE_URL=<url> npx prisma <comando>` na
mesma invocação sem editar `.env`.

package.json confirma `prisma@7.8.0` e `@prisma/client@7.8.0` — os comandos
abaixo usam a sintaxe Prisma 7 (`migrate dev --create-only`, `migrate resolve
--applied`, `migrate diff --from-config-datasource --to-schema=prisma/schema.prisma --script`).

**CORREÇÃO PÓS PLAN-CHECK (2026-07-11)**: a sintaxe originalmente escrita
neste plano (`--from-schema-datasource ... --to-schema-datamodel ...`) NÃO
EXISTE no Prisma 7.8.0 instalado neste repo — confirmado rodando `npx prisma
migrate diff --help` ao vivo. A sintaxe correta e testada é
`npx prisma migrate diff --from-config-datasource --to-schema=prisma/schema.prisma --script`
(compara o estado físico do banco indicado por `DATABASE_URL` contra o
schema declarado no arquivo). Todas as ocorrências deste comando nas Tasks 2
e 4 abaixo já foram corrigidas para essa sintaxe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover @@ignore dos 11 modelos satélite</name>
  <files>prisma/schema.prisma</files>
  <action>
    Em cada um dos 11 modelos listados no bloco &lt;interfaces&gt; acima,
    remover a linha `@@ignore` e atualizar o comentário acima da declaração
    do modelo de "Managed by raw SQL in src/lib/&lt;arquivo&gt;.ts. Model exists
    for schema visibility only." para "Baseline: tabela física já existe em
    produção (ver migration baseline_satellite_tables_schema11). Store em
    src/lib/&lt;arquivo&gt;.ts ainda usa raw SQL até migração store-a-store
    (SCHEMA-02)." Não alterar nenhum campo, @map, @@map, @@index ou @@unique
    — os 11 modelos já refletem exatamente as tabelas físicas (confirmado
    por teste de restore em BACKUP-RESTORE.md, 30 tabelas incluindo as 9+2
    satélite). Não tocar em nenhum outro modelo do arquivo.
  </action>
  <verify>
    <automated>grep -c '@@ignore' prisma/schema.prisma | grep -qx 0 && echo OK</automated>
  </verify>
  <done>Nenhuma ocorrência de @@ignore resta no arquivo; os 11 modelos mantêm todos os demais campos/atributos intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Gerar e validar migration baseline contra qlmed_dev</name>
  <files>prisma/migrations/*_baseline_satellite_tables_schema11/migration.sql, prisma/migrations/migration_lock.toml</files>
  <read_first>.env (confirmar que DATABASE_URL já aponta para qlmed_dev, per server-hardening Phase 2 — NÃO editar este arquivo, apenas ler para confirmar)</read_first>
  <action>
    Com o `.env` atual do repo (já repontado para `qlmed_dev` desde a Phase 2
    do workstream server-hardening), rodar:
    `npx prisma migrate dev --create-only --name baseline_satellite_tables_schema11`.
    Isso usa o shadow database automático do Prisma (requer privilégio
    CREATEDB na mesma instância — já confirmado disponível, pois `qlmed_dev`
    foi criado do mesmo jeito) para gerar o SQL de diff, e cria a pasta
    `prisma/migrations/&lt;timestamp&gt;_baseline_satellite_tables_schema11/`
    SEM aplicar nada em `qlmed_dev` (flag --create-only). Em seguida, como as
    11 tabelas já existem fisicamente em `qlmed_dev` (restauradas do dump de
    produção), rodar `npx prisma migrate resolve --applied
    &lt;nome_completo_da_pasta_gerada&gt;` para registrar a migration como
    aplicada em `qlmed_dev` SEM executar o SQL gerado (que tentaria recriar
    tabelas já existentes e falharia com "already exists"). Depois, validar
    ausência de drift comparando o schema declarado contra o estado físico
    real de `qlmed_dev`: `npx prisma migrate diff --from-config-datasource
    --to-schema=prisma/schema.prisma --script`
    — o output deve ser vazio (nenhum CREATE/ALTER), confirmando que os 11
    modelos batem exatamente com as tabelas físicas de `qlmed_dev`. Se o
    diff não for vazio, o script gerado (ou os modelos) tem um mismatch —
    parar e reportar, não prosseguir para produção. Por fim, `npx prisma
    migrate status` deve reportar "Database schema is up to date!" sem
    migrations pendentes.
  </action>
  <verify>
    <automated>npx prisma migrate status 2>&1 | grep -qi "up to date" && echo OK</automated>
  </verify>
  <done>Pasta de migration criada e commitável; migration marcada como aplicada em qlmed_dev; migrate diff contra schema.prisma retorna vazio (zero drift); migrate status limpo.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Aprovar aplicacao da baseline em producao</name>
  <action>Revisar o resultado da Task 2 (drift zero contra qlmed_dev, migrate status limpo) e decidir se a migration baseline deve ser marcada como aplicada tambem no banco de producao antes da Task 4 rodar.</action>
  <decision>Aplicar a migration baseline (marcar como já aplicada, sem recriar nada) contra o banco de PRODUÇÃO do QLMED</decision>
  <context>
    A Task 2 provou o padrão contra `qlmed_dev`: migration gerada, drift
    zero, `migrate status` limpo. O próximo passo roda `prisma migrate
    resolve --applied` contra o `DATABASE_URL` real de produção (lido de
    `/srv/qlmed/app/env/app.env`, no mesmo host). Este comando NÃO executa
    nenhum DDL — apenas grava um registro na tabela `_prisma_migrations` de
    produção dizendo "esta migration já está aplicada". Nenhuma tabela é
    criada, alterada ou apagada. Ainda assim, é uma mudança de estado de
    metadados de migração em produção e, por regra de segurança desta fase,
    exige aprovação humana explícita antes de prosseguir — mesmo sendo
    "só resolve, não recria".
  </context>
  <options>
    <option id="proceed">
      <name>Prosseguir</name>
      <pros>Fecha SCHEMA-01: Prisma Client passa a reconhecer as 11 tabelas em produção; desbloqueia a migração store-a-store (11-02+)</pros>
      <cons>Toca _prisma_migrations de produção (reversível manualmente via migrate resolve --rolled-back se necessário, mas é uma ação real)</cons>
    </option>
    <option id="abort">
      <name>Abortar</name>
      <pros>Zero risco imediato</pros>
      <cons>SCHEMA-01 fica pendente; nenhuma tabela satélite pode ser migrada para Prisma Client tipado</cons>
    </option>
  </options>
  <resume-signal>Digite "proceed" para aplicar em produção, ou "abort" para parar aqui (Task 4 não será executada)</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Registrar baseline em produção e validar (somente após aprovação da Task 3)</name>
  <files>prisma/schema.prisma</files>
  <read_first>/srv/qlmed/app/env/app.env (ler apenas para extrair DATABASE_URL de produção — nunca imprimir o valor completo em logs; usar diretamente na variável de ambiente do comando)</read_first>
  <action>
    Só executar esta task se a Task 3 tiver resumido com "proceed". Extrair
    `DATABASE_URL` de `/srv/qlmed/app/env/app.env` e usá-lo inline (nunca
    editar `.env` do repo dev). Rodar:
    `DATABASE_URL="&lt;url_producao&gt;" npx prisma migrate resolve --applied
    &lt;mesmo_nome_de_pasta_da_task_2&gt;`. Depois validar drift zero:
    `DATABASE_URL="&lt;url_producao&gt;" npx prisma migrate diff
    --from-config-datasource --to-schema=prisma/schema.prisma --script`
    (esperado: output vazio). Depois:
    `DATABASE_URL="&lt;url_producao&gt;" npx prisma migrate status` (esperado:
    "Database schema is up to date!"). Por fim, rodar `npx prisma generate`
    (sem DATABASE_URL de produção — usa o `.env` padrão do repo) para
    regenerar o Prisma Client local com os acessores tipados dos 11
    modelos, e `npm run build` para confirmar que o build do Next.js
    continua passando (nenhum código ainda usa os novos acessores, isso é
    escopo do plano 11-02).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>_prisma_migrations de produção contém a migration baseline aplicada; migrate diff/status limpos contra produção; prisma generate e npm run build passam sem erro.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor→_prisma_migrations (produção) | Comando de metadados de migração tocando produção; sem input externo/usuário envolvido, mas mudança de estado irreversível sem intervenção manual |
| executor→schema.prisma | Mudança de arquivo versionado, sem risco de runtime até deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | _prisma_migrations (produção) | mitigate | Checkpoint humano bloqueante (Task 3) antes de qualquer comando `migrate resolve`/`migrate` contra produção; drift verificado com `migrate diff` antes e depois |
| T-11-02 | Repudiation | migration.sql gerado | accept | Arquivo fica versionado em git com nome descritivo (`baseline_satellite_tables_schema11`), rastreável no histórico de commits |
| T-11-03 | Denial of Service | `prisma migrate dev --create-only` contra qlmed_dev (shadow db) | accept | Shadow db é efêmero e criado/descartado pelo próprio Prisma; nenhuma tabela real de qlmed_dev é tocada por --create-only |
</threat_model>

<verification>
1. `grep -c '@@ignore' prisma/schema.prisma` retorna 0.
2. `npx prisma migrate status` (contra qlmed_dev, .env padrão) reporta "Database schema is up to date!".
3. `DATABASE_URL=<producao> npx prisma migrate status` reporta o mesmo.
4. Contagens de linhas nas 11 tabelas em produção idênticas antes/depois (nenhum DDL foi executado, apenas metadados).
5. `npm run build` passa.
</verification>

<success_criteria>
- Os 11 modelos satélite existem no schema.prisma sem `@@ignore`.
- Migration baseline registrada como aplicada em `qlmed_dev` e em produção, sem recriar nenhuma tabela.
- Zero drift reportado por `prisma migrate diff` em ambos os ambientes.
- Build e `prisma generate` funcionando com os novos acessores tipados disponíveis (ainda não usados pelo código de aplicação — isso é 11-02+).
</success_criteria>

<output>
Create `.planning/phases/11-unifica-o-de-schema/11-01-SUMMARY.md` when done
</output>
