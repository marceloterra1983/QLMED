---
phase: 06-api-validation-logging
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/auth-options.ts
  - src/lib/auto-sync.ts
  - src/lib/bootstrap.ts
  - src/lib/cnpj-lookup.ts
  - src/lib/cnpj-monitor.ts
  - src/lib/env.ts
  - src/lib/local-xml-sync.ts
  - src/lib/ncm-lookup.ts
  - src/lib/prisma.ts
  - src/lib/product-aggregate-updater.ts
  - src/lib/product-registry-store.ts
  - src/lib/receita-nfse-client.ts
  - src/lib/receita-nfse-sync.ts
  - src/lib/sefaz-client.ts
  - src/lib/xml-file-store.ts
autonomous: true
requirements: [API-03]

must_haves:
  truths:
    - "Zero console.log/warn/error calls remain in src/lib/"
    - "All logging in lib files uses structured pino logger with appropriate levels"
  artifacts:
    - path: "src/lib/auto-sync.ts"
      provides: "Auto-sync module with structured logging (38 calls replaced)"
      contains: "import.*logger"
    - path: "src/lib/local-xml-sync.ts"
      provides: "Local XML sync with structured logging (32 calls replaced)"
      contains: "import.*logger"
  key_links:
    - from: "src/lib/auto-sync.ts"
      to: "src/lib/logger.ts"
      via: "import"
      pattern: "import.*logger.*from.*@/lib/logger"
    - from: "src/lib/local-xml-sync.ts"
      to: "src/lib/logger.ts"
      via: "import"
      pattern: "import.*logger.*from.*@/lib/logger"
---

<objective>
Replace all 102 console.log/warn/error calls in src/lib/ with structured pino logger.

Purpose: The lib/ modules are the core business logic — auto-sync (38 calls), local-xml-sync (32 calls), and 13 other modules. Converting these first establishes the logging pattern.
Output: All 15 lib files use structured logging, zero console.* remaining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-api-validation-logging/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace console calls in high-volume lib files (auto-sync, local-xml-sync, product-aggregate-updater)</name>
  <files>src/lib/auto-sync.ts, src/lib/local-xml-sync.ts, src/lib/product-aggregate-updater.ts</files>
  <action>
For each file:
1. Add `import { createLogger } from '@/lib/logger'` at top
2. Create module-specific logger: `const log = createLogger('auto-sync')` (or appropriate module name)
3. Replace console calls with appropriate log levels:
   - `console.log(...)` → `log.info(...)` (or `log.debug(...)` for verbose/progress messages)
   - `console.warn(...)` → `log.warn(...)`
   - `console.error(...)` → `log.error(...)`
4. Where console.log includes structured data (objects, counts), use pino's object-first pattern:
   - `console.log('Processed', count, 'items')` → `log.info({ count }, 'Processed items')`
   - `console.error('Failed:', error)` → `log.error({ err: error }, 'Failed')`
5. For progress/loop logging (common in auto-sync), use `log.debug` level so it's quiet in production but visible with LOG_LEVEL=debug

Files and approximate call counts:
- auto-sync.ts: 38 calls (heaviest — progress logs, sync status, error handling)
- local-xml-sync.ts: 32 calls (file processing progress, errors)
- product-aggregate-updater.ts: 8 calls (batch processing stats)
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && grep -c "console\.\(log\|warn\|error\)" src/lib/auto-sync.ts src/lib/local-xml-sync.ts src/lib/product-aggregate-updater.ts 2>/dev/null | grep -v ":0$" && echo "FAIL: console calls remain" || echo "PASS: zero console calls"</automated>
  </verify>
  <done>auto-sync.ts (38), local-xml-sync.ts (32), product-aggregate-updater.ts (8) — all 78 console calls replaced with structured logger</done>
</task>

<task type="auto">
  <name>Task 2: Replace console calls in remaining 12 lib files</name>
  <files>src/lib/auth-options.ts, src/lib/bootstrap.ts, src/lib/cnpj-lookup.ts, src/lib/cnpj-monitor.ts, src/lib/env.ts, src/lib/ncm-lookup.ts, src/lib/prisma.ts, src/lib/product-registry-store.ts, src/lib/receita-nfse-client.ts, src/lib/receita-nfse-sync.ts, src/lib/sefaz-client.ts, src/lib/xml-file-store.ts</files>
  <action>
For each of the 12 files:
1. Add `import { createLogger } from '@/lib/logger'`
2. Create module-specific logger: `const log = createLogger('module-name')`
3. Apply same replacement rules as Task 1:
   - console.log → log.info or log.debug
   - console.warn → log.warn
   - console.error → log.error
   - Use pino object-first pattern for structured data

Call counts per file:
- auth-options.ts: 6 (login events — use log.info for success, log.warn for failures)
- bootstrap.ts: 2
- cnpj-lookup.ts: 1
- cnpj-monitor.ts: 1
- env.ts: 2 (startup config logs — use log.info)
- ncm-lookup.ts: 1
- prisma.ts: 1
- product-registry-store.ts: 1
- receita-nfse-client.ts: 2
- receita-nfse-sync.ts: 1
- sefaz-client.ts: 3
- xml-file-store.ts: 3

Total: ~24 calls across 12 files.

IMPORTANT: auth-options.ts has login-related logging (PIN attempts, session events). These should be log.info for successful logins and log.warn for failed attempts (with IP if available), consistent with SEC-01 requirements from Phase 1.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && grep -rl "console\.\(log\|warn\|error\)" src/lib/ --include="*.ts" && echo "FAIL: console calls remain in lib" || echo "PASS: zero console calls in src/lib/"</automated>
  </verify>
  <done>All 15 lib files use structured pino logger, zero console.log/warn/error remaining in src/lib/</done>
</task>

</tasks>

<verification>
- `grep -r "console\.\(log\|warn\|error\)" src/lib/ --include="*.ts"` returns no results
- `grep -r "import.*logger" src/lib/ --include="*.ts" | wc -l` returns 15
- `npm run build` passes (no import errors)
</verification>

<success_criteria>
- Zero console.log/warn/error calls in src/lib/ (was 102)
- All 15 lib files import from @/lib/logger
- Each module uses createLogger with descriptive module name
- Log levels appropriate: debug for progress, info for events, warn for issues, error for failures
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-validation-logging/06-02-SUMMARY.md`
</output>
