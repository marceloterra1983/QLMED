---
phase: 06-api-validation-logging
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - src/lib/schemas/product.ts
  - src/lib/schemas/nsdocs.ts
  - src/lib/schemas/contacts.ts
  - src/lib/schemas/receita.ts
  - src/app/api/products/anvisa/route.ts
  - src/app/api/products/anvisa/bulk-import/route.ts
  - src/app/api/products/anvisa/sync-registry/route.ts
  - src/app/api/products/anvisa/upload-opendata/route.ts
  - src/app/api/products/auto-classify/route.ts
  - src/app/api/products/bulk-update/route.ts
  - src/app/api/products/import-types/route.ts
  - src/app/api/products/rebuild-aggregates/route.ts
  - src/app/api/products/rename-fiscal/route.ts
  - src/app/api/products/rename-manufacturer/route.ts
  - src/app/api/products/rename-type/route.ts
  - src/app/api/products/sync-anvisa/route.ts
  - src/app/api/nsdocs/config/route.ts
  - src/app/api/nsdocs/import-period/route.ts
  - src/app/api/nsdocs/sync/route.ts
  - src/app/api/contacts/cnpj-monitor/route.ts
  - src/app/api/contacts/nickname/route.ts
  - src/app/api/contacts/override/route.ts
  - src/app/api/onedrive/connections/[id]/validate/route.ts
  - src/app/api/receita/nfse/config/route.ts
  - src/app/api/access-log/route.ts
autonomous: true
requirements: [API-01]

must_haves:
  truths:
    - "Product bulk-update validates array of product updates with Zod"
    - "NSDocs config route validates NSDoc credentials before saving"
    - "All POST/PUT/PATCH routes in the codebase validate input"
  artifacts:
    - path: "src/lib/schemas/product.ts"
      provides: "Zod schemas for product routes"
      exports: ["bulkUpdateSchema", "autoClassifySchema", "renameSchema"]
    - path: "src/lib/schemas/nsdocs.ts"
      provides: "Zod schemas for NSDocs routes"
      exports: ["nsdocsConfigSchema", "importPeriodSchema"]
  key_links:
    - from: "src/app/api/products/bulk-update/route.ts"
      to: "src/lib/schemas/product.ts"
      via: "import schema"
      pattern: "import.*Schema.*from.*@/lib/schemas/product"
---

<objective>
Add Zod validation to batch 2 of POST/PUT/PATCH routes: products (12 routes), nsdocs (3 routes), contacts (3 routes), onedrive, receita, access-log.

Purpose: Complete API-01 coverage. After this plan, every POST/PUT/PATCH route in the codebase validates input.
Output: Remaining 20+ routes validate input with Zod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-api-validation-logging/06-01-SUMMARY.md
@.planning/phases/06-api-validation-logging/06-03-SUMMARY.md
@src/lib/schemas/common.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain schemas and validate product routes (12 routes)</name>
  <files>src/lib/schemas/product.ts, src/lib/schemas/nsdocs.ts, src/lib/schemas/contacts.ts, src/lib/schemas/receita.ts, all product route.ts files listed</files>
  <action>
READ each route file first to understand actual body shapes.

1. Create **src/lib/schemas/product.ts**:
   - Read all 12 product POST routes to understand their body shapes
   - Create schemas: bulkUpdateSchema (array of updates), autoClassifySchema, renameSchema (shared for rename-fiscal, rename-manufacturer, rename-type), syncAnvisaSchema, importTypesSchema, etc.
   - For FormData routes (upload-opendata), create field-only schemas

2. Create **src/lib/schemas/nsdocs.ts**:
   - Read nsdocs/config, import-period, sync routes
   - Create schemas for NSDocs credentials, import period params, sync params

3. Create **src/lib/schemas/contacts.ts**:
   - Read contacts/cnpj-monitor, nickname, override routes
   - Create schemas for contact operations

4. Create **src/lib/schemas/receita.ts**:
   - Read receita/nfse/config route
   - Create schema for NFS-e config

5. Apply Zod validation to ALL product routes:
   - Same pattern as Plan 04: safeParse at top, apiValidationError on failure, use parsed.data

6. Apply validation to nsdocs, contacts, onedrive, receita, access-log routes

**Product routes (12):**
- products/anvisa (POST), anvisa/bulk-import (POST), anvisa/sync-registry (POST), anvisa/upload-opendata (POST)
- products/auto-classify (POST), bulk-update (POST/PUT), import-types (POST)
- products/rebuild-aggregates (POST), rename-fiscal (POST), rename-manufacturer (POST), rename-type (POST)
- products/sync-anvisa (POST)

**Other routes (8):**
- nsdocs/config (POST), import-period (POST), sync (POST)
- contacts/cnpj-monitor (POST), nickname (POST), override (POST)
- onedrive/connections/[id]/validate (POST)
- receita/nfse/config (POST)
- access-log (POST)

IMPORTANT: Same rule as Plan 04 — make fields optional when uncertain. Do NOT break existing frontend.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All remaining 20+ POST/PUT/PATCH routes have Zod validation. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final audit — verify 100% coverage</name>
  <files>(no new files — audit only)</files>
  <action>
Run a comprehensive audit to verify all POST/PUT/PATCH routes have Zod validation:

1. List all POST/PUT/PATCH handlers:
   `grep -rn "export.*async.*function.*\(POST\|PUT\|PATCH\)" src/app/api --include="*.ts"`

2. For each, verify safeParse or .parse is present in the same file:
   `for f in $(grep -rl "export.*async.*function.*POST\|PUT\|PATCH" src/app/api --include="*.ts"); do grep -l "safeParse\|\.parse(" "$f" > /dev/null || echo "MISSING: $f"; done`

3. If any routes are missing validation, add it.

4. Also verify the 5 routes that ALREADY had Zod (from before this phase) still work:
   - src/app/api/contacts/cnpj-status/route.ts
   - src/app/api/customers/route.ts
   - src/app/api/invoices/route.ts
   - src/app/api/suppliers/route.ts

5. Run `npm run build` to confirm everything compiles.

6. Run `npm run lint` to catch any lint issues introduced.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && MISSING=$(for f in $(grep -rl "export.*async.*function.*POST\|PUT\|PATCH" src/app/api --include="*.ts"); do grep -q "safeParse\|\.parse(" "$f" || echo "$f"; done) && test -z "$MISSING" && echo "PASS: 100% coverage" || echo "FAIL: $MISSING"</automated>
  </verify>
  <done>Every POST/PUT/PATCH route in src/app/api/ has Zod validation. npm run build and lint pass.</done>
</task>

</tasks>

<verification>
- Every POST/PUT/PATCH route has safeParse or .parse
- `npm run build` passes
- `npm run lint` passes
- Zero catch(e: any) remains (from Plan 03)
- Zero server-side console.* remains (from Plans 02+03)
</verification>

<success_criteria>
- 100% of POST/PUT/PATCH API routes validate input with Zod schemas
- All schemas live in src/lib/schemas/ (reusable, not inline)
- Invalid payloads return HTTP 400 with descriptive Portuguese error messages
- Build and lint pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-validation-logging/06-05-SUMMARY.md`
</output>
