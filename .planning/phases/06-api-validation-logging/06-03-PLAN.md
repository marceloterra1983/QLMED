---
phase: 06-api-validation-logging
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/app/api/nsdocs/import-period/route.ts
  - src/app/api/nsdocs/config/route.ts
  - src/app/api/nsdocs/sync/route.ts
  - src/app/api/onedrive/auth-url/route.ts
  - src/app/api/onedrive/connections/[id]/validate/route.ts
  - src/app/api/onedrive/connections/[id]/route.ts
  - src/app/api/users/route.ts
  - src/app/api/users/[id]/route.ts
  - src/app/api/users/pending-count/route.ts
  - src/app/api/estoque/entrada-nfe/[invoiceId]/route.ts
  - src/app/api/estoque/entrada-nfe/route.ts
  - src/app/api/estoque/import-e509/route.ts
  - src/app/api/receita/nfse/config/route.ts
  - src/app/api/financeiro/contas-receber/invoice/[invoiceId]/installments/route.ts
  - src/app/api/financeiro/contas-receber/invoice/[invoiceId]/route.ts
  - src/app/api/financeiro/contas-receber/route.ts
  - src/app/api/financeiro/contas-pagar/override/route.ts
  - src/app/api/financeiro/contas-pagar/invoice/[invoiceId]/installments/route.ts
  - src/app/api/financeiro/contas-pagar/invoice/[invoiceId]/route.ts
  - src/app/api/financeiro/contas-pagar/route.ts
  - src/app/api/invoices/[id]/route.ts
  - src/app/api/invoices/[id]/details/route.ts
  - src/app/api/invoices/[id]/pdf/route.ts
  - src/app/api/invoices/route.ts
  - src/app/api/invoices/upload/route.ts
  - src/app/api/invoices/backfill-tax/route.ts
  - src/app/api/invoices/bulk-download/route.ts
  - src/app/api/companies/route.ts
  - src/app/api/products/anvisa/route.ts
  - src/app/api/products/anvisa/bulk-import/route.ts
  - src/app/api/products/sync-anvisa/route.ts
  - src/app/api/products/auto-classify/route.ts
  - src/app/api/products/bulk-update/route.ts
  - src/app/api/products/route.ts
  - src/app/api/products/list/route.ts
  - src/app/api/products/details/route.ts
  - src/app/api/products/history/route.ts
  - src/app/api/products/rebuild-aggregates/route.ts
  - src/app/api/products/import-types/route.ts
  - src/app/api/products/settings/route.ts
  - src/app/api/certificate/upload/route.ts
  - src/app/api/certificate/info/route.ts
  - src/app/api/dashboard/route.ts
  - src/app/api/suppliers/route.ts
  - src/app/api/suppliers/details/route.ts
  - src/app/api/customers/route.ts
  - src/app/api/customers/details/route.ts
  - src/app/api/contacts/cnpj-status/route.ts
  - src/app/api/cnpj/[cnpj]/route.ts
  - src/app/api/cte/manifest/route.ts
  - src/app/api/fiscal/by-cfop/route.ts
  - src/app/api/fiscal/dashboard/route.ts
  - src/app/api/ncm/bulk-sync/route.ts
  - src/app/api/ncm/[code]/route.ts
  - src/app/api/ncm/refresh/route.ts
  - src/app/api/anvisa/validate/route.ts
  - src/app/api/nsdocs/documents/route.ts
  - src/app/api/webhooks/n8n/route.ts
  - src/app/api/reports/valvulas-importadas/route.ts
  - src/app/api/reports/valvulas-importadas/pdf/route.ts
  - src/middleware.ts
  - src/app/error.tsx
  - src/app/global-error.tsx
  - src/app/(painel)/error.tsx
  - src/components/LotEditModal.tsx
autonomous: true
requirements: [API-02, API-03]

must_haves:
  truths:
    - "All 34 catch(e: any) blocks converted to catch(e: unknown) with apiError()"
    - "Zero console.log/warn/error calls remain in src/app/api/"
    - "No error response leaks stack traces or internal details"
  artifacts:
    - path: "src/app/api/invoices/upload/route.ts"
      provides: "Invoice upload with proper error handling and logging"
      contains: "apiError"
    - path: "src/app/api/products/bulk-update/route.ts"
      provides: "Bulk update with proper error handling"
      contains: "catch.*unknown"
  key_links:
    - from: "src/app/api/**/route.ts"
      to: "src/lib/api-error.ts"
      via: "import apiError"
      pattern: "import.*apiError.*from.*@/lib/api-error"
    - from: "src/app/api/**/route.ts"
      to: "src/lib/logger.ts"
      via: "import logger"
      pattern: "import.*logger.*from.*@/lib/logger"
---

<objective>
Fix all 34 catch(e: any) blocks and replace all 92 console.log/warn/error calls in API routes, middleware, and remaining files.

Purpose: Standardize error handling so no internal details leak to clients, and complete the console.* replacement across the entire codebase.
Output: All API routes use apiError() helper and structured logger. Zero console.* and zero catch(e: any) in codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-api-validation-logging/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix catch blocks and replace console calls in API routes (55 files)</name>
  <files>All route.ts files listed in files_modified above</files>
  <action>
For each of the 55 API route files that have catch(e: any) OR console.log/warn/error:

**catch block fixes (27 files, 34 occurrences):**
1. Add `import { apiError } from '@/lib/api-error'` at top
2. Replace `catch (e: any)` with `catch (e: unknown)`
3. Replace the catch body's manual error response with `return apiError(e, 'route-name')`:
   - BEFORE: `catch (e: any) { console.error('Error:', e); return NextResponse.json({ error: e.message || 'Erro' }, { status: 500 }) }`
   - AFTER: `catch (e: unknown) { return apiError(e, 'POST /api/invoices/upload') }`
4. If the catch block has special handling (e.g., returning 400 for specific error types), preserve that logic but still use `catch (e: unknown)` with proper narrowing:
   - `if (e instanceof ZodError) return apiValidationError(e)`
   - `return apiError(e, 'context')`

**console replacement (55 files, 86 occurrences):**
1. Add `import { createLogger } from '@/lib/logger'` at top
2. Create route-specific logger: `const log = createLogger('api/route-name')`
3. Replace console.log → log.info (or log.debug for verbose), console.warn → log.warn, console.error → log.error
4. Use pino object-first pattern for structured data

**Combined files (some files need BOTH fixes):**
Files needing both catch fix AND console replacement include: users/route.ts, certificate/upload/route.ts, invoices/upload/route.ts, products/bulk-update/route.ts, and others. Handle both changes in a single pass per file.

IMPORTANT: Do NOT touch `src/lib/` files — those were handled in Plan 02.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && echo "=== catch(e: any) ===" && grep -rn "catch\s*(e:\s*any)" src/app/api --include="*.ts" | wc -l && echo "=== console calls in api ===" && grep -rn "console\.\(log\|warn\|error\)" src/app/api --include="*.ts" | wc -l && echo "(both should be 0)"</automated>
  </verify>
  <done>Zero catch(e: any) and zero console.log/warn/error in src/app/api/. All routes use apiError() and structured logger.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console calls in middleware, error boundaries, and components</name>
  <files>src/middleware.ts, src/app/error.tsx, src/app/global-error.tsx, src/app/(painel)/error.tsx, src/components/LotEditModal.tsx</files>
  <action>
Handle the 5 remaining non-api, non-lib files:

1. **src/middleware.ts** (1 call):
   - Import createLogger, replace console call
   - Use `const log = createLogger('middleware')`

2. **src/app/error.tsx** (1 call), **src/app/global-error.tsx** (1 call), **src/app/(painel)/error.tsx** (1 call):
   - These are client-side error boundary components
   - KEEP console.error here — these run in the browser where pino is not available
   - Add a comment: `// console.error intentional — client-side error boundary, pino not available`
   - This is the ONE exception to the "zero console" rule

3. **src/components/LotEditModal.tsx** (2 calls):
   - This is a client component (runs in browser)
   - Same as error boundaries — KEEP console calls with comment if they're genuinely client-side
   - If the console calls are in a fetch error handler that could be removed (just show toast instead), remove them

After this task, the ONLY console.* remaining in the codebase should be in client-side error boundaries (3 files) with explicit comments explaining why.
  </action>
  <verify>
    <automated>cd /home/marce/QLMED/dev && echo "=== remaining console calls ===" && grep -rn "console\.\(log\|warn\|error\)" src --include="*.ts" --include="*.tsx" | grep -v "// console\.\(log\|warn\|error\) intentional" | grep -v "node_modules" | wc -l && echo "(should be 0 or only error boundaries)"</automated>
  </verify>
  <done>All server-side console calls replaced. Only client-side error boundaries retain console.error with explanatory comments.</done>
</task>

</tasks>

<verification>
- `grep -rn "catch\s*(e:\s*any)" src/ --include="*.ts"` returns 0 results
- `grep -rn "console\.\(log\|warn\|error\)" src/ --include="*.ts" --include="*.tsx" | grep -v intentional | grep -v node_modules` returns 0 or only error boundary files
- `npm run build` passes
</verification>

<success_criteria>
- Zero catch(e: any) in entire codebase (was 34)
- Zero console.log/warn/error in server-side code (was 194, minus 3 client error boundaries)
- All API routes use apiError() for error responses
- No stack traces or internal details in error responses
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-validation-logging/06-03-SUMMARY.md`
</output>
