import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Contas a Receber | QLMED' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
