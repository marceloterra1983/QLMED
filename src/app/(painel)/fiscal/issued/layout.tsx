import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'NF-e Emitidas | QLMED' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
