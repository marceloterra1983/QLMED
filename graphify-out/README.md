# Graphify snapshot

These generated artifacts are the durable Graphify snapshot produced on
2026-07-23. They are useful for historical navigation, but they are not a
source of truth for current behavior: `graph.json` records a local source
commit that is not present in the canonical clone, and `main` has advanced
since the build.

Use the application code and canonical documentation for current decisions.
Rebuild the graph with Graphify before making freshness-dependent claims, and
do not edit `graph.json`, `graph.html`, `manifest.json` or `GRAPH_REPORT.md` by
hand.
