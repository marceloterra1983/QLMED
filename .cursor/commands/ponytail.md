---
description: Roda Ponytail no QLMED (audit YAGNI do checkout canônico)
---

Execute the project skill `ponytail-qlmed` (`.cursor/skills/ponytail-qlmed/SKILL.md`). Extra text after the command is the scope: omit for whole-repo audit; `review` for the branch diff vs origin/main; `lite`/`full`/`ultra` for intensity; a path to narrow the scan.
