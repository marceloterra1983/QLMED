---
description: Sempre subir produção ao terminar; sem preview persistente no host
alwaysApply: true
---

# Produção ao terminar

Quando a tarefa do QLMED fechou (PR mergeado no `main`, ou mudança operacional
já autorizada), **suba produção**. Não pergunte “quer deploy?”.

Não subir Next persistente neste host (`:3002` / `qlmed-dev-preview` /
`.worktrees/preview` foram retirados). UI valida em
`https://app.qlmed.com.br` depois do deploy.

## Deploy produção

1. Espere o CI `ci.yml` **push/main** verde no SHA de 40 caracteres de `origin/main`.
2. Garanta env necessário sem imprimir `.env` (ex.: VAPID para Web Push).
3. Despache com `bash scripts/deploy-production.sh <SHA>` (usa
   `QLMED_DEPLOY_GH_TOKEN`). **Não** use o `gh` default do Cloud Agent —
   o token da GitHub App Cursor só tem `actions:read` e devolve 403 em
   `workflow_dispatch`.
4. Se o environment `production` pedir aprovação, aprove (`GH_TOKEN=$QLMED_DEPLOY_GH_TOKEN gh …` / `current_user_can_approve`).
5. Espere o workflow terminar verde. Health: `http://127.0.0.1:13000/api/health` com o SHA novo.
6. Informe a URL do deploy e o SHA. Não cole segredo.

Se `QLMED_DEPLOY_GH_TOKEN` estiver ausente: peça o secret no environment
Cloud (PAT fine-grained, Actions=RW no QLMED) e **não** declare o deploy
feito.

Se a tarefa criou migration Prisma, o **mesmo PR** tem de atualizar
`scripts/verify-production-migration-window.cjs` (`EXPECTED_MIGRATION` + SHA do
`migration.sql`) e o assert em `scripts/test-production-migration-window.cjs`.
Sem isso o deploy recusa a janela e a migration não aplica.

Proibido: terminar só no GitHub; menu “quer que eu suba?”; deploy com CI vermelho;
force-push em `main`; imprimir `production/env/app.env`; recriar o preview
persistente `:3002` neste host.
